# ERA Heterogeneity Addendum

Diagnostic join for TASK A release-fit outputs.

- Clusters: `23`.
- Flight-level LOAO pushes over gate for every held-out flight: `True`.
- Interpretation: LOAO pushes over gate across all left-out flights; no single ERA-specific rescue. Per-era strata are mostly <6; phase6 family is the only diagnostic stratum with >=6 clusters.

Artifacts: `forced_withhold_samples_with_era.csv`, `flight_loao_with_era.csv`, `era_loao_audit.csv`, `era_family_stratified_fit.csv`, `era_heterogeneity_summary.json`.
