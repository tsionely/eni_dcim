# CRITERION REGISTRATION — Shadow-fit decision structure (committed BEFORE the shadow-fit evidence exists)

Channel-2 amendment on RESPONSE-48 §4: the branch structure, split,
and interpretation must exist in a committed criterion artifact
before the shadow-fit evidence is generated. The shadow-fit
artifact's generator commit must carry THIS commit as an ancestor
(manifest-checker enforced). No shadow-fit artifact exists at
registration time; the QA instruction to produce one points at this
file.

## Scope

DIAGNOSTIC only, by standing law: this fit decides whether the
policy-attenuation mechanism is sufficiently explanatory to justify
advancing the repair through both-channel approval and
shipping-build revalidation. It does not release sigma_a, validate
a maximum age, or close any board row by itself.

## The decision branches (structure only; no numbers projected)

- **U95_shadow <= 0.35** (most conservative valid bound, full
  registered tier at 23 approaches): the candidate mechanism is
  sufficient for the repair-gate question; the path proceeds to
  re-earning on the repaired shipping build per the release
  sequence (mechanism closes -> both-channel repair approval ->
  repair ships -> full restamps on shipping semantics -> full-tier
  fit on that build -> twelve-row manifest walk).
- **U95_shadow > 0.35**: do not move; the remaining structure is
  NAMED and pre-registered with a falsifiable prediction before
  anything else advances.
- **b0_new distribution**: concentrated near zero with the low-auth
  discovery cluster as an isolated remainder -> local structure
  remains plausible; broad same-sign spread -> a second mechanism
  is hunted with the same falsifiable form the first received.

## Reporting requirements (binding on the artifact)

Three-way split, published separately: the 5 discovery approaches,
the 18 confirmatory approaches, the pooled 23. **Row-3 closure
reads the 18-approach confirmatory distribution** (discovery found
the mechanism; only out-of-sample closes it); the pooled set is
context. Per approach: auth_at_latch, v_latch, predicted offset,
b0_old, b0_new, remainder, regime labels; plus profile-likelihood
and cluster-bootstrap bounds, LOAO, and cluster-balanced coverage —
side by side with the old-path (defect-documentation) numbers.
Independent evidence counts in APPROACHES (5, 18, 23), never rows.
The old-vs-shadow delta is described as a PAIRED ARCHIVE-SCALE
DIAGNOSTIC EFFECT — never a shipping-build benefit, flight
validation, or release statistic.
