# RESPONSE 35 — P4(d) classified HONEST on the recorded numbers; the fixture is shipped; the census stops honestly at 5

## 1. P4(d): the pre-registered honest branch, with a sharper ending than either branch predicted

QA's diff (d918221) reproduced the discrepancy exactly (115 raw
fixes both arms; 64 vs 56 accepted; lost frames 308-315) and every
clause of the ruled pass tree is evidenced in the table:

- Same lock epoch at the start; the contradiction is exposure-level
  and PERSISTENT — 47-55 unique contradictory exposures accumulated
  before the losses, not a one-frame clear.
- The rejected detector ranges sit at **17.2-19.9 m against a live
  prior lock of 0.08-0.58 m** (mismatch 17-19 m vs the 0.4-relative
  threshold ~0.03-0.23 m): scale/bearing evidence of an actual
  different target, orders of magnitude past marginal.
- And the tree's last clause turns out to be the headline: **the
  detector-only arm ACCEPTED those eight rows** — far-gate quads
  attributed as certified metrology to the near-gate identity at
  sub-meter range. The diff did not merely price the certificate
  boundary; it caught the OLD acceptance committing the exact
  wrong-gate misattribution the boundary exists to prevent. The
  parallel arm is not "explained safety-neutral"; it is a measured
  safety improvement with zero honest-liveness cost.

Verdict as pre-registered: HONEST RELOCK. F4's cluster quarantine
CLEARED by the classifier that was named before the evidence. The
ruled regression fixture is SHIPPED (189/189): the
prediction-consistency gate is pinned on the recorded F4 numbers
(frames 308/309/315 refused; a same-gate fix anchors; a no-prior
first lock always may), plus the consequence chain — relock revokes,
and below the promote floor the dead identity reaches at most
PROBATION, never metrology again on that approach. Board rows 1 and
2 now read green from our side; both tanks' read closes them. The
"preferred ownership boundary" rearchitecture (source-local SIDE
certificate + identity arbiter) belongs to the false branch and is
NOT triggered; the honest branch keeps the current wiring with its
new fixture.

## 2. The §5 log list is now complete in the runtime

The dual-read previously recorded the shadow RATE only. Every
SIDE-authority tick now logs the full forecast pair — old/new
e_cross, old/new command (the same guidance step run twice, scalar
math, nothing actuates), and delta_latch = v_anchor_old − v_full_raw
(the definition-proof sign convention). TermStatus carries the pair;
the fixture pins that the e_cross split equals the crossing_error
difference of the two rates exactly. This is the instrument that
exposes a command-step or liveness regression in the repair BEFORE
any live change.

## 3. Census: 5 of 6 — stopped honestly again; one flight owed, one failure named

Nine flights swept: archive F2 (9 rows), F4 (12, now cleared), F6
(2) + metrology f2 (7) and f3 (8) are legal approaches; metrology
f1 FAILED as a cluster — 11 FULL fixes below 3.5 m, ZERO e_z-usable,
SIDE row-only/uncertified throughout (reason
FULL_BELOW_3P5_NOT_EZ_USABLE — the same signature as archive
20260720T071220). The profile itself is sound (its two siblings
produced legal clusters), so the shortfall rule applies as written:
**fly 6 − 5 = 1 more metrology flight**, while the analyst
diagnoses f1 in parallel — was certification never earned (late
descent below the promote floor? bloom? a relock mid-approach?), or
is a harness filter over-rejecting? If f1 is rescued by diagnosis
we reach 7 clusters; if flight 4 fails with f1's signature, the
diagnosis becomes blocking before flight 5.

## 4. Board

Rows 1-2 (P4(d) classified + fixtured; SIDE shown not to corrupt
FULL authority — it corrected it): GREEN pending both tanks' read.
Rows 3-9 await the 6-cluster harvest (one flight out); the v2.1 run
at 6 clusters uses the 6-7 tier as ruled — profile-likelihood U95
primary, exhaustive LOAO, bootstrap supplementary, cluster count
reported BY AGE BIN, mechanism test with the exact delta_latch
convention and b0 regime-invariance. Row 10 (R26-2/3) maps under
those artifacts. Rows 11-12 standing. Cohort-4 HOLD unchanged.
