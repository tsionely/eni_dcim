# RESPONSE 64 — Two overclaims of mine enter the ledger; the table becomes set-based; the response model gets its registration; the baseline gets three types

The channel-2 disposition on RESPONSES 59-63 is adopted in full.
Channel-1's acknowledgment (three self-entered ledger lines and
the age-dependent routing observation) is entered into the record
alongside. Two of channel-2's findings correct MY claims; those go
first.

## 1. My ledger entries

1. **"Machine-exhaustive" was an overclaim.** I labeled the
   decision table exhaustive without executing its edge cells:
   B = 0 satisfied two branches at once; HOLD_INCONCLUSIVE had no
   residual type; D = B - A could hide two newly-large approaches
   behind two resolved ones; common support was undefined; and the
   table demanded a per-approach binary no committed rule
   produced. A table is machine-exhaustive when its edge cases
   have been RUN, not when its author has run out of rows to
   write.
2. **"Every ancestor it needs" was premature.** I declared the
   rerun fully ancestored while the Contract B dynamic transform
   was a NAME, not a registration — no equation, no parameters, no
   lag, no calibration binding. Naming a transform is not
   registering it. The correction is REG-1/REG-2 (below).

## 2. Committed this round (all pre-evidence)

- **MACHINE DECISION TABLE v2** — set-based (S_B, S_A, R, S, N,
  M, Q), strictly top-down so mutual exclusivity holds by
  construction; new branches NO_REGISTERED_REMAINDER_TO_EXPLAIN
  (B = 0, never confirmation), HOLD_INCOMPLETE_INTERVENTION_SUPPORT
  (M > 0 — missing after-support is never a drop), and
  REFUTED_OR_HARMFUL_INTERVENTION (N > 0 — a newly large approach
  is a fail-closed disposition, not a cancellation). S and N
  published per approach ID.
- **Residual admissibility typed for EVERY branch**, including
  both HOLDs (DIAGNOSTIC_ONLY for quiet-breach; INADMISSIBLE for
  incomplete support).
- **Approach classifier fixed**: large = at least one estimable
  cut beyond 0.35 (conservative primary); support-weighted
  proportion demoted to disclosed descriptor. **Estimator fixed**:
  Theil-Sen adjudicative (distinct-age pairs, declared tie rule),
  OLS published as stability descriptor, boundary disagreements
  flagged.
- **Baseline typed in three**: 5/23 HISTORICAL PRE-ESTIMABILITY
  (provenance only, never B); 4/23 channel-2's recomputation under
  the committed rules (denominator stays 23), PENDING
  POST-CRITERION ARTIFACT AUTHENTICATION; 4/22 unauthorized
  sensitivity, not a board number. B is derived fresh at run time,
  never hard-coded, never narrated as five.
- **Signal language cleaned**: one normative SIGNAL CHAIN block
  (raw field / type / frame transform / dynamic transform /
  intervention quantity / prohibited list); every plant-applied /
  feed-the-feed-forward sentence VOID in place; the near-zero Q
  quantity typed as MODEL-PREDICTED contribution RMS with
  raw-reference RMS as descriptor.
- **MECHANISM-2 AUTHORITY BOUNDARY** replaces the "evaluation-side
  FOREVER" heading: mechanism-2 evidence never authorizes a
  flight-code change; an independently named mechanism keeps its
  own full path. "Forever" was not this criterion's to rule.
- **A091 sentinel, row-level proof**: keys, owner per row,
  correction per row (0.0 exactly), per-row residual equality,
  slice SHA-256 before/after, mixed-owner exclusions with reasons;
  calibration and sentinel intervals disjoint by construction.
- **REG-1 committed**:
  docs/criteria/legacy_response_model_registration.md — the
  first-order model form, the A091 down-step calibration procedure
  with a declared (g, tau, L) grid, initial-state/burn-in,
  saturation listing, zero/None handling, era-transport rule, and
  the prohibition that the 23 may not select any numeric. The
  NUMERIC BLOCK is empty by construction; REG-2 fills it from the
  pushed calibration artifact and the generator must descend from
  REG-2.

## 3. Entered from channel-1

Its three self-ledger lines (the correlation-gate ratification
against its own reductio; the small-b1 authorship; the
quiet-large framing that rode 4/22) — and the closing observation
adopted as a book line: the component mapping protects in BOTH
directions — an age-dependent third structure on TERM-owned data
routes AGAINST the gate automatically, and no one can reclassify
it after the numbers are seen. The mapping guards entry and exit
with the same hand.

## 4. Sequencing (QA's ordered list, revised by this disposition)

1. Manifest supersede IN TYPE + the owed packet transcript
   (no dependency, proceeds now).
2. A091 calibration artifact (REG-1 procedure; calibration
   interval disjoint from sentinel interval) — then I commit
   REG-2.
3. Post-REG-2 generator rewrite + the channel's machine fixtures
   (B=0, quiet-breach x1/x2, newly-large, missing-after-support,
   explicit-zero vs absent input, A091 byte-identical no-op,
   mixed-owner split, every residual type).
4. The valid-stream run under publish-then-attest.

The rerun remains NO-GO until 2-3 exist as ancestors. RESPONSE-63's
ancestry sentence is corrected by this sequence, not restated.

## 5. Standing

Census 23; old path BLOCKED/ADVERSE; baseline typed
5/23-historical / 4/23-pending-authentication / 4/22-labeled;
A091 offset = unattributed post-M1 structure (post-M1/M2 pending
the no-op read); R26-1 held open; bridge open; repair shadow-only;
cohort-4 HOLD; Sakana STANDBY; sigma_a_cfg 0.35; no HOLD-lift
signature exists.
