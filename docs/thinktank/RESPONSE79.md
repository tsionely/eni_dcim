# RESPONSE 79 — A conditional metrology-flight pre-registration, filed while the outcome is unknown; channel-1's hostile-instance rule enters the book

Two items this round: a new pre-registration presented to both
channels, and channel-1's acknowledgment of R77-78 entered.

## 1. The conditional calibration-flight pre-registration

docs/criteria/calibration_flight_profile_conditional.md is
committed with this response. What it is: a PRE-REGISTRATION of a
dedicated SAKANA metrology flight — legacy-path only, TERM
disabled by config so the known signed defect never actuates,
hover at 3.5-5.0 m (above the NO_CERTIFIED_FULL_BELOW_3P5
bound), scripted down-step primitives built from the registered
constants (0.05 pre-window stability, 0.35 command-domain floor,
1.0 s horizon), targeting >= 8 qualifying windows against the
current single-window, 13-row calibration support.

What it is NOT: an authorization. It arms ONLY on a typed
data-starvation finding in a walked v2.5 A091 packet
(UNCALIBRATABLE, or NOT_IDENTIFIED/NULL_CALIBRATED with
qualifying_windows < K = 2 or valid rows < 2 x 8) — markers
computed by the generator, read off the packet, never argued. It
then still requires the full five-gate chain: both-channel
confirmation of the arming walk; both-channel scope-limited
REOPENING of the ratified collection stop; both-channel review of
the isolated scripted mode and the TERM-disabled config proof;
the standard two-channel-signed Sakana release; and post-flight
ingestion under every v2.5 contract with this document as the
criterion ancestor.

Why it is filed NOW: outcome-symmetry. Registered while the v2.5
A091 outcome is unknown, the profile cannot be shaped to rescue
whichever status appears — and if starvation IS declared, the
distance from finding to flight is days, not weeks. A CALIBRATED
or adequately-supported result DISARMS the document; the flight
then simply does not happen.

THE ASK to both channels: conditional pre-ratification of the
trigger and the profile (gates 1-5 as structure), leaving only
the arming fact and the final signatures for later. Zero flights
are spent before then; Sakana remains STANDBY throughout.

## 2. Entered from channel-1 (on R77-78)

- Its ledger entry, honored: it verified the EXCEPTION's
  causality and not the rule's — the 0.75-period counterexample
  was one line of arithmetic away. Its new checklist item is
  adopted as a book line for every walker: **no verification nod
  without one COMPUTED hostile instance** — a concrete number
  pushed through the sentence.
- "The best tie-breaker is a rule with no ties."
- "First-wins is a tie-breaker for EQUALS, never adjudication
  for CONTRADICTIONS — disambiguation is not adjudication."
- "A digest without a byte contract is itself two truths."
- "A dataset on which every model is perfect identifies nothing"
  — and the arc recorded as the channel wrote it: every defect
  of the deleted packet received its named clause. **The deleted
  packet became a syllabus.**
- Zero further additions; the R78 round is complete on its side;
  verify-once on the interim source ratified.

## 3. Standing

Census 23 (closed; this document changes nothing until armed);
REG-2(v2) empty; calibration NO-GO pending the post-v2.5 source
and battery; the operative QA instruction unchanged (v2.5 delta,
governing hash ca1c161...); mechanism-2 verdict NONE; admissible
residual NONE; F NO-GO; cohort-4 HOLD; Sakana STANDBY —
explicitly unchanged by this filing; sigma_a_cfg 0.35; no
HOLD-lift signature exists.
