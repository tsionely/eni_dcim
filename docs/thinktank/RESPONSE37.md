# RESPONSE 37 — The one-line answer to §1.1 is NO: the deep archive was never in the pool

## 1. The census pool, audited

The nine-flight sweep covered EXACTLY the phase6l cohort-3 six
(flight_ids 20260720T0710xx-0715xx) plus the four phase7m metrology
flights. The fixtures tree on disk holds 60+ recording directories
spanning phase1 through phase7m — including the deep eras the
question names: phase5b-confirm through phase5f-terminal, phase6b
aligned-dash through phase6i rate-A/B (ten runs per arm, twice),
and cohort-2 in both its block-A and redo forms. None of it was
milled through the current perception build; the retroactive SIDE
stream has never been generated for any pre-6l recording. **The
cluster farm may indeed already be on disk, and the shortfall may
close with zero flights and zero new censoring.**

The full-archive sweep is tasked (instruction issued with this
note): enumerate every fixture directory carrying real-resolution
frames + IMU; mill each through the CURRENT build with retroactive
SIDE production; apply the UNCHANGED cluster predicate (new
physical approach epoch, legal anchor, usable FULL e_z, parallel
SIDE, legal maintenance interval, provenance, no P4 contamination);
report the expanded census — with the ruled era/regime/provenance
columns — BEFORE any fit. Independence law unchanged: one approach
= one cluster, cut points travel together, flight-level LOAO as the
heterogeneity audit, and if era shows up in LOAO the fit STRATIFIES
rather than surrenders. Flight 5's design decision stays parked
until this sweep reports: the recorded world is mined before the
physical one is spent.

## 2. The two companion instruments, tasked with it

- **Discriminator table (§1.2)**: legal vs censored approaches
  against observable profile features (approach speed, gap
  incidence, entry geometry, lateral offset, bloom exposure,
  descent timing vs the promote floor). The expanded census
  multiplies the labeled rows this table learns from; the
  tanks-visible profile decision — IF flight 5 is still needed —
  reads off it, not off intuition.
- **Coverage-censoring ledger (§1.3)**: the per-label zero-usable-
  e_z rates (SCALE_GATE_FLIPPED_FAR_GATE_POST_GAP,
  NO_CERTIFIED_FULL_BELOW_3P5, plus whatever the f4 autopsy and the
  archive sweep add), promoted to a named artifact in the lift
  package's context sheet — it bounds cohort-4 treatment
  availability and harvest rate simultaneously.

## 3. Standing state

f4 autopsy proceeds with F1/F5 comparators, unprejudged. R26-3
physical-regime inventory proceeds (the phase6i rate-A/B and
phase6g/6h terminal-live recordings now in scope are exactly where
physically-realized TERM commands would live — the same sweep
serves both instruments). sigma_a_cfg 0.35 for first live,
reaffirmed. Collection stopped; Sakana STANDBY; census 5/6 pending
the archive answer. Cohort-4 HOLD.
