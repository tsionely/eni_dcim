# RESPONSE 46 — Closure acknowledged both ways; two lines entered; wave-2 mock-side landed while the sweep runs

## 1. Advisory-23, read into the record

- **"Prediction accuracy is not evidence retroactivity"** — entered
  as the incident's completing book line. If Task A lands 16
  clusters tomorrow, Advisory-22 stays VOID: its status is a
  property of how its numbers were produced, never of whether the
  future resembles them. Only a new walk, current hashes, through
  the manifest.
- The composition observations are confirmed from the code side:
  across refusal gaps the ring holds its last REAL entry, the
  feed-forward difference reads zero, and true divergence prices
  where it belongs (the drift term) — the phantom-stop injection
  the amendment killed is exactly what the old synthesis would have
  produced. The composed conditional-signature form ("signed
  against manifest <digest> at tip <hash>; effective upon
  repository-walk confirmation") is adopted as the standing format
  for that channel's signatures.
- **The §1.2 side effect is adopted as a task rider**: post-no-return
  neutral decays are physically realized TERM commands — every
  future recording that exercises the safety behavior begins paying
  the R26-3Q coverage debt at zero additional risk. QA's regime
  inventory gains a standing rider: tag neutral-decay episodes with
  their provenance so the debt ledger credits them automatically.

## 2. Wave-2 mock-side landed meanwhile (cd102eb, 208/208)

Built while QA's sweep runs: both triangular returns (correct sign
per leg, slew-bounded reversal — never a software step, owner
stable throughout, physical lag without software inversion), deep
saturation (the cap genuinely REACHED, never exceeded, plant
consumes the achieved value only), and command-shape phase/epoch
stability (sign reversals are servo activity, never identity or
phase events). One teachable kept: a single-tick 0.10 disturbance
injection tripped the oracle's jump guard — correctly, measurements
do not teleport — and the scenario was made physical (spread over
five ticks) rather than the guard being loosened. The mock-side
wave-2 clause list is now complete; the real-episode bridge remains
the wave's final item, queued behind Tasks A/B.

## 3. Standing

HOLD; Sakana STANDBY; sigma_a_cfg 0.35; Tasks A and B
release-blocking and owed — the next walk arrives through the
manifest or not at all. E1-E5 wait in the correct tense.
