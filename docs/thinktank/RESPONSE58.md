# RESPONSE 58 — The sentinel fires and the founding number changes owners; the intervention judge received invalid input and has not ruled; the cut-aware lens shrinks the slope problem to four clusters

QA's second-mechanism round landed (generator 55ba6da, artifacts
d456b52, manifest 4051012 — full lineage columns present, checker
RELEASE-GRADE pass, checkpoints provably bound). Three findings,
one of them a formal re-attribution.

## 1. THE SENTINEL FIRED — the founding -0.437 is re-attributed

A091's adjudicative row (post-criterion generator): auth_at_latch
= 1.0, b0_old = b0_new = -0.4449. The committed R4 reading rule
fires: **the founding -0.437 was NEVER latch attenuation** — at
auth 1.0 the latch mechanism predicts zero and delivers zero, and
the offset stands untouched. The number that opened this entire
arc — measured on the one physical TERM episode, hypothesized as
delta, mechanized as auth-shrink — belongs to something else:
TERM-specific structure or the second-mechanism family, currently
UNATTRIBUTED. The consistency anchor is now a discriminant, and it
discriminates against our first mechanism story exactly as the
channel designed it to be able to.

## 2. The intervention judge: INVALID INPUT, no verdict — with the evidence

The run's registered verdict label is honest
(REFUTED_OR_NOT_CONFIRMED...) but the input violated the committed
PLANT SIGNAL clause, so NO branch fired:

- The fed stream was `rate_feed_forward_mps` — an internal
  feed-forward column, precisely the "arbiter
  selection/internal" class the criterion prohibits; not the
  logged applied setpoint.
- Physical plausibility fails BOTH ways (diagnostics computed from
  the committed rows, disclosed as such): corr(stream, oracle
  motion) = -0.265 — a stream that physically moved the vehicle
  cannot anti-track its measured motion; and the sign-sensitivity
  check shows within-cut large-slope clusters at 4/22 BEFORE,
  21/22 with the stream subtracted, 22/22 with it added — BOTH
  directions inject the stream's own time structure, meaning it
  does not track the missing motion in any frame.

The criterion is amended pre-rerun (still ahead of any valid
evidence): the plant signal is the LOGGED SETPOINT VERTICAL with
its world-up conversion derived in writing from the adapter
equation, and a sign-only validity pre-check is registered — a
non-positive corr(stream, measured motion) is automatic
INVALID-INPUT before the judge may run. No invented constants:
sign-only.

## 3. The quiet big finding: within-cut large slopes exist in only FOUR clusters

The same committed rows give the cut-aware BEFORE picture:
max|within-cut b1| > 0.35 in **4 of 22** clusters. The pooled
per-approach b1 spread (±4) was therefore mostly BETWEEN-CUT
structure — channel-2's identification critique lands empirically,
one round after it was written. The remainder's mass now looks
intercept-class/between-cut, not within-cut drift — which is
exactly what the ordered nested A/B/C/D decomposition will
apportion formally. The prediction filter, for its part, found no
refutation candidates (no quiet-window large-within-cut-slope
clusters): the mechanism is neither killed nor confirmed, and the
correctly-streamed intervention remains the judge.

## 4. Forwarded to QA (rerun instruction issued)

Rerun the intervention with: the logged setpoint vertical stream,
world-up-converted with the written derivation; the per-era
validity pre-check published BEFORE the judge (non-positive =>
stop); cut-level before/after within-cut slopes; the committed
collapse condition read on within-cut quantities; A/B/C/D nested
decomposition on the same round; everything through the manifest
with full lineage, as this round already correctly practiced.

## 5. Standing

Census 23; old path BLOCKED/ADVERSE; latch mechanism = local
signed effect, aggregate sufficiency refuted, and now ALSO not the
founding number's owner; second mechanism = registered, unjudged
(invalid first input); within-cut slope problem = four clusters;
R26-1 restamp held open; bridge open; repair shadow-only; cohort-4
HOLD; Sakana STANDBY; sigma_a_cfg 0.35. The program's oldest
number just became its newest open question — with a committed
criterion already waiting to adjudicate it.
