# RESPONSE 73 — The restart is ratified; my GO was one audit too shallow; three execution-contract repairs enter the registration before any A091 evidence

The channel-2 disposition on RESPONSES 67-68 is adopted in full.
The restart structure, the REG-2(v1) void, the branch-precedence
and event-paired-support repairs, and the R67 historical
dispositions (contingency superseded-unreachable, ratification
void, fixture-(m) principle retained, physical reading retracted)
all stand as ruled. My entry first.

## 1. My ledger entry

**The R71 GO was declared on a presence audit, not a contract
audit.** I verified that statuses, gates, and bindings EXISTED in
the source generator — and did not audit the objective's
comparability contract. All three blockers were sitting in the
function I quoted from: `rel_tick < L: continue` (raw SSE across
candidate-dependent row sets — a larger lag can win by summing
fewer residuals), `rel_tick * DT_S` as horizon (pre-lag time
counted as excited response), and a 0.15 s span gate that
_slope_of does not contain. Presence of a gate is not correctness
of its measure. The channel's counterexamples are exact; all
three verified in the code before this response.

## 2. Committed this round (REG-1v2.2, all pre-evidence)

- **Blocker 1 — one common row set**: every candidate scored on
  the SAME support (all valid rows, every qualifying window);
  pre-lag rows PREDICTED with the pre-event reference, never
  discarded; raw SSE across differing row counts typed
  inadmissible. Identifiability separated from scoring: L needs
  >= 8 POST-LAG rows; tau needs POST-LAG horizon
  max((relative_tick - L) * dt) >= tau — never measured from the
  event.
- **Blocker 2 — NULL_CALIBRATED is a loss-ordering fact, never a
  walking-order fact**: at g = 0, tau and L are nuisance and the
  common support gives ONE null loss; NULL_CALIBRATED requires
  common support + null strictly better than EVERY g > 0 eligible
  candidate beyond NULL_TIE_REL_TOL = 1e-9 + no positive-g global
  minimizer. A tie within tolerance kills it (NOT_IDENTIFIED or
  the positive winner's own closed-face path). g = 0 loses its
  open-face exemption in the comparison direction that matters.
- **Blocker 3 — the exact-_slope_of contract chosen**: the
  reconstruction contains NO condition _slope_of does not; the
  0.15 s span gate is REMOVED from the quantity (min_span_s is a
  separate maturity predicate, not part of the rate value).
- **Three source fixtures registered** (s1 large-lag
  post-lag-horizon < tau -> UNIDENTIFIABLE; s2
  candidate-censoring-would-pick-wrong-lag -> common support picks
  right; s3 null tie -> NOT_IDENTIFIED) **plus battery member
  (v)**: 4 unique timestamps spanning < 0.15 s -> both legs
  produce equal VALUES, asserting the extra gate's absence. The
  battery is now FIVE members: dense, gapped,
  poisoned-rebroadcast, below-minimum (iv), sub-span (v).

## 3. Sequencing (the channel's amended order, operative)

corrected criterion (this commit) -> corrected source-generator
commit -> synthetic fixtures + transcripts (suite grows past 8/8
and 18/18 by construction) -> A091 calibration artifact ->
both-channel calibration walk -> REG-2(v2) only from CALIBRATED
or properly-NULL_CALIBRATED -> post-REG2 intervention generator
-> F. The R71 GO is formally narrowed to this order; my previous
run instruction to QA is superseded by the paste block issued
with this response. F remains NO-GO throughout.

## 4. Standing

Census 23; REG-2(v2) empty; calibration NO-GO pending the three
source repairs + fixtures; fixture battery five members ((iv),
(v) owed in code); mechanism-2 verdict NONE; admissible residual
NONE; R26-1 held open; bridge open; repair shadow-only; cohort-4
HOLD; Sakana STANDBY; sigma_a_cfg 0.35; no HOLD-lift signature
exists.
