# RESPONSE 20 — The retro column reads N=1; the thin budget's harvest path; dual readiness shipped

Both advisory-14 offline actions are done or routed; the honest
number leads.

## 1. The retro column: N = 1

Replaying cohorts 2 + 2-REDO + 3 under the corrected semantic with
the FULL admission verdict (registered bounded reading): exactly ONE
window converts — one exposure in phase6l F2 (the 11-clip flight)
where old-ready=0, new-ready=1, and admission passes. Under the
registered bounds: the bookkeeping muted the treatment axis in one
window; it was NOT the main thief. The dominant fact sits beside it:
**10 of 18 flights had ZERO gate-passing accepted exposures inside
the engagement range at all** — after identity, scale, and
shadow-mode gates, the final 2m are a coverage VOID in the recorded
feature streams. The mute-axis record re-attributes to coverage
starvation, which is precisely the ladder's mandate — and precisely
what L1 must measure.

## 2. The thin budget — the harvest path, sharpened

Acknowledged and computed: at the analog sigmas the side rung's floor
is 0.272 against the 0.30 corridor — ceremonial, not working. One
consequence discovered while planning the harvest: the recorded
FEATURE LOGS cannot mature the rung's row — they carry the old tags
and the old filtering (the void of §1). The earned-sigma harvest
must re-run PERCEPTION on the recorded VIDEO (vision.aigprec per
flight; replay mode exists) so the tracker emits fresh SIDE_PAIR
features through the new door. The L1 harness is therefore specified
as a perception replay, and its two outputs stand as you wrote:
accuracy fixture + the rung's first library row. Cohort 4 flies on
earned sigmas or the side rung stays shadow — a fourth mute cohort
from a third cause is the outcome this order of operations exists to
prevent.

## 3. Shipped this round

- **Dual-computed readiness**: TermStatus now records `ready_legacy`
  (whole-history semantic, decision-free) beside the governing
  fresh-tail readiness — the semantic's A/B rides cohort 4 at zero
  flight cost.
- The SIDE_PAIR structural-immunity line is in the design doc as
  ruled; Block A vs Block B membership (owned-zero-command counts
  for A; nonzero-command is B's currency) recorded for the cohort-4
  report template.
- Codex's interim A/B on the pre-ladder build (1/10 captures, one
  v_bz sign-flip-jitter run) is noted; the meaningful mock round is
  the ladder build with the L1 perception-replay harness, tasked.
