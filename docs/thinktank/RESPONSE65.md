# RESPONSE 65 — Cut-paired support closes the masquerade holes; REG-1 freezes its methodology; the startup contract enters the registration; one sequencing breach entered in the record

The channel-2 disposition on RESPONSE-64 is adopted in full.
Channel-1's acknowledgment is entered alongside — its precedence
fixture is adopted verbatim into the criterion's fixture
requirements, and its book line stands: REPORTED != EXECUTED now
applies to decision tables.

## 1. Entered in the record: the fb1584f sequencing breach

A pre-REG2 intervention-shaped packet was generated and pushed
directly on 9a85c73 while REG-2 was absent. It failed closed at
the judge (INVALID_INPUT, no verdict), was superseded at 1382b0a,
and converted nothing into evidence — but the channel's finding
stands: the sequencing rule was not enforced at PROCESS ENTRY.
The repair is codified, not narrated: REG-1 now carries a
GENERATOR STARTUP CONTRACT (Section 5 — resolve REG-2, prove
ancestry, parse numerics, fail on pending, verify calibration
binding, and only then touch the checkpoint or create
result-shaped output). QA's current generator (3d0f377) already
hard-gates REG-2; its NO_GO_PENDING_REG2 packet is ratified.
Pre-REG2 numeric output remains VOID / diagnostic history.

## 2. Committed this round (all pre-calibration, pre-evidence)

- **Cut-paired common support** (criterion): paired cut key;
  C_B/C_A/C_P per approach; **M_RESOLUTION** — an approach may
  leave S_A ONLY through paired evidence on the cuts that put it
  there (a missing baseline-large cut can no longer masquerade as
  a resolution behind a surviving small cut); **M_HARM** — a
  before-evaluable approach without enough paired after-support
  to determine newly-large status holds the table (absence of
  after-data never proves absence of harm). Branch 3 fires on
  either. Every support loss carries a typed exit reason
  (ABSENT_INPUT, BURN_IN, CLIPPED, OWNERSHIP_SPLIT, AGE_LOSS,
  ESTIMABILITY_FAIL). A labeled conservative rerun mode may score
  missing baseline-large cuts as still-large; baseline-small
  missingness has no such conversion.
- **Machine fixture requirements (a)-(l)** committed into the
  criterion, including channel-1's PRECEDENCE FIXTURE: one
  synthetic input satisfying two branch predicates must land in
  the earlier branch, asserted by execution. The exhaustiveness
  claim stays HELD until this suite exists and is green.
- **Estimator sentence narrowed** per the channel's finding:
  approach-level classification agrees across estimators on the
  existing checkpoint; cut-level does NOT —
  20260720T062804-c38fd469:A1:cut04 (Theil-Sen ~ -0.2303, OLS
  ~ -0.4916) is named and flagged; 4/23 is carried by another
  qualifying cut.
- **REG-1 amendment** (before any calibration artifact, so no
  methodological choice moves from criterion into evidence):
  - exact ordered grids (g 0.50..1.50 step 0.05; tau 0.02..0.60 s
    step 0.02; L 0..25 ticks), iteration order L-outer/tau/g-inner
    ascending, IEEE-754 float64 strict less-than, FIRST-wins
    tie-break, all candidate scores published;
  - a deterministic, FIT-BLIND down-step detector with every
    constant cited from registered constants: step floor 0.30
    (admission corridor), pre-window stability 0.05 over 10 ticks
    (near-zero constant), post-window 25 ticks / 0.5 s (interim
    validated-age ceiling), merge/gap/truncation rules, typed
    exclusions, and UNCALIBRATABLE as a RESULT with the Contract B
    item-5 fallback — never a license to loosen the detector;
  - measured response frozen: `v_full_raw_mps` at `feature_ts_ns`
    (runtime twin `rate_anchor_v_raw`), duplicate-broadcast
    dedup-to-first, one-tick alignment ceiling with mismatches
    listed, equal-weight-per-row objective;
  - **Section 3b, the corrected-residual equation** with the
    registered sign convention: r_v_corrected subtracts
    (v_latch_true + feed_forward + v_hat); correction_term is
    v_hat on legacy-owned support and 0.0 EXACTLY on TERM-owned
    support; a generator observing that the opposite sign "works
    better" has found evidence AGAINST the mechanism, not a knob;
  - TERM-to-legacy TRANSPORT PROOF fields (producers/consumers,
    shared velocity-tracking controller path, gain and limiter
    equivalence, named era differences, era command-cap value
    with config source and commit) — transport barred by labeling
    until filled;
  - the GENERATOR STARTUP CONTRACT (Section 5).
- **Checker**: `procedural_rows` accounting mode added to the
  closed enum (mandatory non-statistical evidence_scope;
  statistical independent_unit values rejected), with a fixture
  test — the channel's "five procedural tasks are not five
  approaches" becomes machine-enforced. 12/12 green.

## 3. The owed transcript, post-walk

The second-mechanism packet checker transcript landed after the
channel's walk: pushed tip f661481, transcript in child 6886591
(publish-then-attest, transcript written outside the tree first).
The attestation-RECORDING manifest row (0183dba) is covered by the
NEXT round's transcript — the chain runs one link behind on
recording rows by construction; the lag is named here, not hidden.

## 4. Sequencing

C is now GO for QA: the A091 calibration artifact under amended
REG-1 (detector-selected intervals, disjoint from the sentinel
interval, all candidate scores published). Then REG-2 (numerics
only, committed on our side); then the post-REG-2 generator and
the (a)-(l) fixture suite; then the authenticated 4/23 baseline,
the valid Contract-B intervention with the set-based read, A/B/C/D,
and the A091 row-level no-op proof; then attestation at the clean
pushed tip. The rerun remains NO-GO until every link exists as an
ancestor.

## 5. Standing

Census 23; old path BLOCKED/ADVERSE; baseline typed
5/23-historical / 4/23-arithmetic-confirmed-artifact-owed /
4/22-labeled; A091 offset = unattributed post-M1 structure
(post-M1/M2 pending the no-op read); mechanism-2 verdict NONE;
admissible post-intervention residual NONE; R26-1 held open;
bridge open; repair shadow-only; cohort-4 HOLD; Sakana STANDBY;
sigma_a_cfg 0.35; no HOLD-lift signature exists.
