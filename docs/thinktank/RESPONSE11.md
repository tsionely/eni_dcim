# RESPONSE 11 — the three numbers (Advisory 6B, same day)

## 1. WARM-cohort true-vertical (the operative blind budget)

**1.27 m** (end and max coincide; P4 rerun, flight 20260716T203450,
0.6s blind window, warm = range5m_to_3m + collision cohort). Your
adverse-signs forecast was 0.9-1.1; actual is slightly worse. Against
the ±0.33-0.5 m margin it is meter-class either way — the retraction
of the Advisory-2 blind budget is accepted and recorded, and the
certificate-gated terminal channel remains the only closure. The
unification is adopted into the ledger: true-vertical = 0.95z' +
0.31x', so the T1 plane filter and side-pair absolute-range chain are
henceforth VERTICAL-channel work items; priority inherited.

## 2. Band-impulse audit of the graze (settles P-A / A6-iii)

**P-A CONFIRMED on this log.** Phase6d F1 dash #2: the airframe
center tracked ~[+0.4, +0.5] above opening center from t=11.95
through 12.17 (pixel-derived; run-in R=2.0 -> 0.74) — squarely inside
the branch-A banner band — with **zero collision events** in that
window. The only gate contacts are nine micro-impulses (0.02-0.86)
clustered in 12.18-12.22s, exactly when the airframe top would reach
the top-bar row, consistent with canopy-skimming the bar underside
(A8 half-extent ~0.3 puts body-top at ~+0.8 there). A solid branch-A
banner would have produced sustained large-area contact through the
band; it produced nothing. Verdict: **branch B / non-solid banner** —
the aperture reframe stands down to a trim, the vertical aim
reference returns to the opening center, and Advisory 5's [A] items
are suspended per your clause.

## 3. h_b from the far-range reference

Assigned and in progress: the far-range reference finally exists
(20260719T134446 a6_far_reference: 108 unique frames, 9.6-19.6m, 62
far detections) and the analyst is running the A6 re-measure on it;
h_b extraction for the border-exit ping constants (A3/H4) is folded
into the same pass, per your rider.

## Shadow status

Already flying: the terminal channel has run in shadow on every
commit tick since phase5e (SHADOW + FEATURE topics in every flight
log), including all phase6c/6d flights. The overlap-band evidence is
accumulating exactly as you specify; the three enable gates stand.

## The ~0.25m unattributed residual (P-B register)

Provisional attribution from the 6d F1 trace: at R=0.74 the q-derived
believed vertical read +0.13 (above center) while pixel-truth read
~+0.5 — a ~0.3m believed-vs-pixel measurement bias at commit speeds
(gyro-only attitude drift under acceleration; kp=0). The altitude
hold servos on the believed value, so the bias passes straight into
the crossing. This is precisely the error class the terminal
channel's pixel-row oracle bypasses. P-B is registered as the default
forecast for the aim-floor-removed cycle now in the air: if it
arrives +0.2-0.3 high, the attribution is confirmed and the
shadow-data/nudge path is the conversion route.

## Ledger

The remaining rank-1 constants — top-up gap, sink-insurance
magnitude, the 0.45 abort threshold, search/retreat climb biases —
are marked PRESUMED GUILTY UNTIL RE-DERIVED in the design contract.
The first name on the list was convicted in flight within one cycle.
