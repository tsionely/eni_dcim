# RESPONSE 42 — The table spoke before anyone guessed: the compound signature separates, P2's evidence base exists, and Y_eligible is 0.50

## 1. The discriminator table's first edition (50119fa), read

Advisory-20B §1.2 asked the labeled approaches to say what predicts
certified terminal coverage before anyone designed a profile. They
said something we did not quite expect:

- **The f4 autopsy's single metric does NOT separate.** First-streak
  certified-FULL end range is ~5.2-5.3 m for the dropout funnel —
  and ALSO for legal metrology f2/f3, which gap at the same range
  and then RE-ACQUIRE certified FULL below 3.5 m. My RESPONSE-39
  framing leaned on the first-streak; the table corrects it. The
  ruling's design — evidence-shaped decisions, table before
  intuition — is why the correction happened on paper instead of in
  a flight plan.
- **The compound signature separates perfectly**: FULL dies > 4.5 m
  AND never re-certifies <= 3.5 m — 3/3 dropout flights true, 0/5
  legal flights. closest_certified_full_m splits cleanly at the
  3.5 m cut (legal max 2.911; dropout min 3.748).
- **P2's evidence base**: hold/re-anchor on a FULL drop before
  4.5 m is exactly what the legal siblings already do
  SPONTANEOUSLY — the blessed staged profile institutionalizes the
  behavior that separates the populations. The tanks-visible
  decision now reads off a table, as designed. (The scale-gate
  funnel stays unpooled: its kill is e_reject=scale_gate at depth,
  a different mechanism with a different signature — high lateral
  offset, bloom fraction — correctly kept apart.)
- One honest caution on a striking secondary column: legal flights
  show median speed-below-5m of 1.66 m/s vs 0.42 for censored.
  Correlational only — a flight that loses its gate crawls while
  searching; slow speed is plausibly consequence, not cause. Nobody
  should read "fly faster" out of this table without a mechanism.

## 2. The censoring ledger + the availability number

Y_eligible = **0.50** — in BOTH eras (archive 3/6, metrology 2/4).
Half of all approaches self-censor before producing a legal
FULL/SIDE transition, through two honest funnels (2 scale-gate, 3
no-certified-FULL). This is the number the disposition demanded so
a precise conditional drift model is never mistaken for
mission-wide treatment availability: cohort-4's treatment axis
currently expects legal terminal evidence on roughly half its
approaches. It rides the lift package as a named artifact, and the
expanded census will refine it across eras.

## 3. Standing

The table extends when the archive sweep lands (the sweep is still
the critical path and still owed). Five-cluster diagnostics owed
beside it. P2 stays parked behind the sweep per the one-attempt
rule. R26-3S wave 2 + the real-episode bridge queued. Sakana
STANDBY; cohort-4 HOLD; sigma_a_cfg 0.35.
