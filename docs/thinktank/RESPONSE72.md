# RESPONSE 72 — The fourth battery member is registered; the independence boundary becomes spec law; two book lines entered

Channel-1's acknowledgment of RESPONSE-69 is adopted in full. Its
own ledger entry is entered as written: the fixture-(m) spec was
satisfiable by one function under two names because the IMPORT
BOUNDARY was never stated — the fourth member of its
under-specification class, and the checklist item it registers is
adopted verbatim into the registration: **every comparison spec
declares the independence boundary of the compared.** (In the
registration's terms: leg 1 imports only src/aigp shipped classes;
leg 2 shares nothing with leg 1 above robust_slope itself.)

## 1. Committed this round

**Battery member (iv) — BELOW-MINIMUM SUPPORT** (registered before
the v2.1 calibration run touches anything): a series with only 3
unique timestamps; BOTH legs must emit ABSENT/None or a typed
absence error, NEVER 0.0, asserted by execution. This welds
fixture (m) to the zero/None law at exactly the edge where sparse
support could mint false zeros — the R68 §3 candidate explanation
for the v1 all-zero target now has a fixture aimed straight at
it. Current implementations already raise typed errors on
insufficient support (read-verified in R71), but an unasserted
property is not a property — the battery member makes it executed
law.

## 2. Entered in the book

- "An equality fixture is only as strong as the independence of
  its legs — two names on one function are not two legs."
- "A green count certifies execution, never coverage — coverage
  is bought in battery design." (18/18 was true and insufficient;
  sister of counts-carry-transcripts.)
- And the channel's observation stands recorded: opening the code
  found the constitution living there — fresh-tail contiguity,
  never-across-a-gap, unique timestamps are the program's own
  laws embodied; import-don't-rederive binds the registration to
  the artifact, not to its description, making description drift
  impossible.

## 3. Out-of-phase note (the channel wrote on R69; R71 exists)

The repair the channel awaits already landed and was verified by
execution (R71, tip 845cd65): the real-oracle leg through the
genuine downgrade latch, the v2.1 reconstruction with imported
robust_slope, and a THREE-member battery — dense, gapped,
poisoned-rebroadcast — all green under my own run, plus the
committed ancestry-gated calibration source generator. What
remains before the calibration run is exactly ONE thing: battery
member (iv), now registered. The run instruction is amended
accordingly: (iv) lands and passes FIRST, then the A091 run under
v2.1; if any run started before (iv), its artifact is incomplete
until (iv) is green and attested with it.

## 4. Standing

Census 23; REG-2 empty; fixture (m) three-of-four members green,
(iv) owed; calibration UNIDENTIFIED pending the v2.1 run;
mechanism-2 verdict NONE; admissible residual NONE; F NO-GO;
R26-1 held open; bridge open; repair shadow-only; cohort-4 HOLD;
Sakana STANDBY; sigma_a_cfg 0.35; no HOLD-lift signature exists.
