# RESPONSE 56 — Both rulings adopted; the thresholds are committed before the table; the R3 audit answers from the code; the sentinel's raw values are already speaking

## 1. Ruling A adopted — with the four riders implemented

- **R1 — committed**: docs/criteria/second_mechanism_refutation_
  thresholds.md registers, before any table or intervention exists:
  near-zero activity = RMS < 0.05 m/s (the registered rate-sign
  deadband / jump-floor constant); large |b1| = > 0.35 m/s^2 (the
  release gate's own constant — b1 carries sigma_a's units, so a
  deterministic slope exceeding the whole stochastic budget is
  large by the gate's definition); K = 2 (the registered
  falsification-breach convention). No threshold touches the
  observed distribution.
- **R2 — adopted as structure**: the correlation table is a FILTER
  (in-sample by construction, can only kill); the INTERVENTION is
  the judge — the evaluation reruns with the recorded legacy
  plant-applied vertical fed to the feed-forward; collapse
  confirms, survival refutes, and the post-intervention residual is
  the admissible drift measurement. The collapse condition is
  committed in the criterion (>= half the >0.35 clusters drop AND
  all near-zero-activity clusters end < 0.35).
- **R3 — answered from the shipped code, declared in the
  criterion**: the runtime rings record TERM CHANNEL-APPLIED values
  only (achieved, prior-tick, never synthetic); legacy verticals
  live in flight logs but feed NO ring — so in never-TERM archive
  replays the model's feed-forward is EMPTY and the legacy
  actuation is unmodeled BY CONSTRUCTION. The mechanism, if
  confirmed, is evaluation-side; the flight code's zero/None law is
  exactly why. The harness's own stream disclosure joins the
  adjudicative round.
- **R4 — the sentinel is already speaking**: A091's raw row in the
  non-adjudicative table reads auth_at_latch = 1.0, delta_latch =
  0, b0_old = b0_new = -0.4449. If the adjudicative round confirms
  those values, the committed reading rule fires: the founding
  -0.437 was NEVER latch attenuation — it re-attributes, and the
  consistency anchor becomes the discriminant between mechanisms.
  Flagged now so nobody is surprised then.
- The cautionary consequence is adopted verbatim: if the second
  mechanism is refuted, 0.35 becomes a contested constant and the
  validated-age machinery re-derives before any release walk.

## 2. Ruling B recorded

The historical wrong-sign verdict is INVALID TEST — voided, never
reinterpreted, regenerating under the committed criterion (the
ancestor now exists). The channel's ownership line and its book
entry — a sign test without a declared reference and deadband is
not yet a test — are entered. The 28's anatomy (near-zero commands
x raw-measurement sign noise, 28/68 -> 0 under registered
semantics) stands as the archaeology's finding; the 24/16
coincidence note stands in the ledger.

## 3. The adjudicative round's scope, restated once

One QA round, one manifest, now carrying: the 3/20/23 closure
tables resampled on the registered twenty; the event-support
wrong-sign re-score (9/7/2) with fixture-level verdict language;
the prediction table WITH its committed thresholds; the
INTERVENTION run (legacy plant-applied fed to the feed-forward)
with post-intervention per-cluster b1; the A091 sentinel row; the
harness stream disclosure; full checkpoint lineage; checker
transcripts through the self-binding contract. Every generator
postdates every criterion it reads — mechanically checkable, and
checked.

## 4. Standing

Unchanged: old path BLOCKED/ADVERSE; repair shadow-only, its live
case resting on the intervention's verdict; census 23; cohort-4
HOLD; Sakana STANDBY; sigma_a_cfg 0.35 — the constant the entire
adjudication now orbits.
