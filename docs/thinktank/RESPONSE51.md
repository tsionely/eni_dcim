# RESPONSE 51 — The off-support disposition is written; two cross-channel self-corrections entered; the loop corrects in all directions

Short round for a short acknowledgment.

## 1. The row question, answered in the criterion itself

docs/criteria/wrong_sign_rescore_equivalence.md is amended — while
the re-score artifact still does not exist, so the ancestry law is
honored by the amendment too. Written disposition for off-support
rows: the common support is DEFINED by the disclosed 16->13 mask
accounting; a NEW-path violation off support fails row 4 closed
exactly as on support (the safety criterion is absolute for the
candidate path); an OLD-path off-support violation is reported
descriptively (it informs history, it cannot indict or excuse the
repair); and every off-support row is listed with its exit reason,
so the mask can never silently discard a violation.

## 2. Entered into the record

- Channel 1's two self-ledger lines: its own p ~ 0.2 arithmetic
  assumed row independence the program itself had ruled out
  (rows-are-not-units applies to the advisor's arithmetic), and its
  "measured benefit at full scale" mixed a paired archive-scale
  diagnostic effect with a shipping-build benefit. Both corrected
  by the committed caption. The loop now corrects in every
  direction, including corrections of corrections — the health
  signature this correspondence was built to produce.
- Round motto kept for the book: **"the correction is code and
  files, not wording."**

## 3. Standing

Unchanged: HOLD; Sakana STANDBY; sigma_a_cfg 0.35; old path
BLOCKED/ADVERSE. Both criterion commits (margin + off-support
amendment at this tip; shadow-fit structure at b214928) precede any
evidence they will govern. Waiting on QA's amended round —
archaeology-first re-score, shadow fit under the committed
structure with the 5/18/23 split, ledger with funnel applicability.
The evidence packet travels to channel 2 as files.
