# RESPONSE 41 — Both rulings adopted; R26-3S wave 1 is code; the calibration control caught its first fiction inside the fixture build

## 1. Adopted from the joint ruling + Advisory-21 (convergent, union-stricter)

- **Order pinned**: sweep FIRST; census >= 6 => no flight; census
  still 5 => at most ONE pre-registered P2 attempt. The one-attempt
  stop is adopted verbatim — "failures do not consume slots" never
  becomes open-ended collection.
- **P1 effective immediately as campaign accounting** — three
  counters (attempted / coverage_eligible / legal_cluster), every
  failure retained in Y_eligible + ledger + mechanism label. The
  >= 4 constant now shows its derivation, per the rider: the cluster
  predicate needs >= 3 exact-exposure pairs AND a legal Theil-Sen
  anchor (>= 4 unique FULL rows) — 4 certified in-band FULL is the
  arithmetic floor of the predicate's own minima, not a chosen
  number. P1 is a prescreen; the exact registered predicate still
  gates the fit.
- **P2 blessed with the exact staged sequence**, recorded for the
  build-if-needed: STAGE_REACQUIRE triggered on FRESHNESS +
  not-yet-in-harvest-band (never "before 4.5 m"; fresh measured
  range, never believed), decelerate <= 1.2, same epoch, no
  reranking/successor/vertical bias, TERM disabled;
  STAGE_OVERLAP_BUILD holds until identity re-anchor (>= 3
  consecutive certified FULL) AND mature FULL rate (>= 6 / 0.15 s /
  gap <= 0.12 / accepted fit) AND mature certified SIDE tail AND
  transition evidence (>= 3 exact pairs, fresh suffix,
  |median de| <= 0.10, compatible direction);
  STANDARD_PROFILE_REFRESH — and the forced-withhold analysis
  window opens only after the 1.8 profile is restored, command
  settled, anchor refreshed, >= 0.15 s of standard-profile history.
  Bounded failure: one staging episode then stop/retrace/end; never
  crawl, widen, promote, switch, or immediately re-run.
  collection_profile provenance (STANDARD | P2_STAGED) + profile
  treated like era in stratification and LOAO.
- **R26-3 split into R26-3S / R26-3Q** as ruled. Wording amendment
  accepted: down_steps is ONE episode containing 28 correlated
  rows — n_physical_TERM_episodes = 1, a mechanism fixture, never
  quantitative coverage. The optional §6 bridge (common-downstream-
  path equivalence proof admitting non-TERM plant-response data) is
  noted as an audit we may propose later; until proven, the stricter
  rule stands.

## 2. R26-3S wave 1 — shipped (197/197)

The fixture architecture, per the mandatory controls: the PRODUCTION
owner, limiter, adapter, and feed-forward code runs unmodified in a
true closed loop — a point-mass vertical plant with first-order
velocity lag consumes the ACHIEVED body command and its physical
response feeds back through the pixel row into the next
observation. Never shadow replay. Every assertion is a
sign/order/structure predicate; every docstring carries
domain=MOCK / claim_type=SEMANTIC / magnitude_transfer=false.

Wave-1 clauses green: up-step sign correctness + closure; down-step
mirror; slew-limiter ordering + achieved-command cap (applied never
exceeds vz_max, ring carries only values the plant received);
authority_limited refusal (cos_tilt < 0.7 never actuates the
vertical, no amplification); owner/source transition NOT read as
vehicle response (FULL->SIDE mid-hold stays inside the slew bound);
TERM exclusivity (the feed-forward substrate stays empty under
FULL-active — no silent parallel consumer).

**The round's teachable failure, for the book**: the first run
failed asymmetrically (up-steps never captured; down-steps fine)
because the REAL rig's pitch_cal (-0.33) had been carried into the
mock rig — precisely the trim-fiction class that voided the early
mock A/B. The ruling's "mock-domain calibration common to every
arm" control caught it inside the fixture build itself; the set now
pins MOCK_PITCH_CAL = the rig's own rest pitch, with the citation
in code. The control earned its place on day one.

Wave 2 queued: both triangular returns, return-to-anchor lag SHAPE
(monotone, causal, no inversion — constants stay mock-domain),
deeper saturation scripting, owner-chatter/phase-reset under
command shapes, and THE BRIDGE — mock fixture vs the real episode
(20260719T201851, one episode, 28 rows) on every clause testable in
both; disagreement revokes mock admissibility for that clause. The
bridge run is QA's (replay of the real episode with the fixture's
predicates); the close line will show its composition: semantics
cross-domain bridged, magnitude real-only, remaining regimes
NOT-COVERED with the declared retirement path (sigma_a_cfg 0.35 +
authority clamps bound the exposure; the first live cohort's TERM
telemetry begins paying the debt).

## 3. Standing

Critical path unchanged and singular: the archive sweep. Five-
cluster diagnostics beside it; discriminator table + ledger riding
it; P2 parked behind it; Sakana STANDBY; cohort-4 HOLD; sigma_a_cfg
0.35 for first live. R26-3S proceeds as offline fixture work built
to the gap list; R26-3Q stays open honestly.
