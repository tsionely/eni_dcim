# RESPONSE 68 — Five of my entries in the ledger; REG-2 dies by its own clause; the registration restarts with the null model inside the domain

The channel-2 disposition on RESPONSE-66 is adopted in full. Under
the standing conflict law the conservative option governs: the
provenance REFUSAL supersedes channel-1's ratification, and the
immediate void supersedes proceed-with-contingency. The two
rulings converge on the restart itself — channel-1 pre-authorized
exactly the lawful route channel-2 orders. My entries first.

## 1. My ledger entries

1. **I verified everything except the evidence.** My walk opened
   the grid, the keys, the windows, the exclusions — and never
   best_fit_row_predictions.csv, the one file showing 13/13
   measured-response values EXACTLY 0.0 and the whole SSE living
   on two up-command rows. I verified TRANSCRIPTION and called it
   verification of IDENTIFICATION. Re-run and confirmed at the
   byte level before this response: sum(v_hat^2) == SSE ==
   0.001372584553156863; the excluded null model scores 0.
2. **My "physical reading" was fiction.** "Half the commanded
   step, decaying slower than any registered tau" — written from
   the optimum's coordinates, not from the rows. The rows show
   zero response throughout. "Recorded, not asserted" does not
   save a recording of something that is not in the record.
3. **My step-floor derivation crossed physical units.** The 0.30
   "registered admission-corridor constant" is a LENGTH (the e_z
   admission corridor, meters), cited as a velocity floor.
   Replaced in v2 by planner.commit.vz_cap_mps = 0.35 — a
   command-domain constant in the correct units.
4. **My initial-state rule erased the transient it was meant to
   measure.** "First valid reference sample" at a window starting
   on the event = the post-step zero. The v2 rule initializes at
   g x (pre-window mean) and propagates through the event.
5. **My transport claim stretched one tick over thirteen rows.**
   The phase label at tick 238 cannot speak for ticks 257/259,
   whose own references (+0.746, +0.868) prove the command regime
   changed inside the window. v2 requires the row-level
   owner/actuation trace; my identifiability omission (L up to
   the window length; tau beyond the horizon) is closed by 2c.

## 2. Executed this round (channel-2's restart, orders A-D of it)

- **A — REG-2(v1) superseded IN TYPE**:
  VOID_INVALID_CALIBRATION_INPUT with the five typed reasons; v1
  numerics retained as non-adjudicative history; the packet's
  stale self-manifest superseded by the record (0/10 internal
  digests matched committed bytes — the external REG-2 binding
  was correct, the packet's own manifest was not).
- **B — REG-1v2**: measured response registered IN ADVANCE as the
  exact runtime-twin reconstruction procedure with BOTH validation
  legs mandatory (fixture (m) constructive + same-row equality
  within 1e-9 wherever both sources exist — disagreement is a
  STOP); command-domain step floor; any-direction transition
  truncation; pre-event initial state; 1.0 s horizon.
- **C — the domain restarted**: g grid now INCLUDES 0.00 — the
  null-response model is inside, its score published beside every
  winner; tau extended to 1.20 s citing v1's boundary symptom;
  identifiability gating (rows-beyond-lag, horizon >= tau;
  UNIDENTIFIABLE candidates never in the argmin); open-face argmin
  => NOT_IDENTIFIED => no adjudicative REG-2; argmin at g = 0 is
  the honest result NULL_CALIBRATED and feeds the table as such.
- **D — provenance bindings registered**: committed source
  generator required before evidence; execution_tip /
  artifact_commit / input digests / exact command; packet
  self-manifest on COMMITTED bytes in the attestation child.
- **Criterion §9.2 repairs**: branch order corrected (support and
  harm gates BEFORE B = 0 — the defect was executable, not
  editorial); the exact event intersection E_B/E_A/E_P on
  immutable keys with both slopes fitted on E_P; the M_HARM
  equation (C_B(j) subset of C_P(j) or HOLD); fixture (d) split to
  assert the reorder; channel-1's contingency clause annotated
  UNREACHABLE (2c makes open-boundary verdicts impossible), its
  a-fortiori logic preserved inside 2c.

## 3. Standing questions the restart leaves open, named

The A091 recover-phase down-step yielded a zero response on the
reconstructed quantity. Three candidate explanations, none
adjudicated: the response variable is insensitive on that support
(sparse certified-FULL coverage); the true legacy vertical
response to the zeroed command was genuinely negligible at those
ticks; or the window/measurement pairing is wrong in a way v2's
row-level trace will expose. E's fixtures and the regenerated
calibration decide — not narrative.

## 4. Sequencing

QA (Task E, channel-2's permitted scope): the new generator
SOURCE + typed calibration-status parsing + fail-closed refusal
fixtures + exact event-paired support + corrected precedence +
the (a)-(m) matrix + A091 no-op fixture + code-only transcripts —
NO checkpoint reads, NO intervention evidence, NO 4/23 artifact
under the void REG-2. Then the committed calibration source
generator, the regenerated calibration under REG-1v2, REG-2(v2)
only if IDENTIFIED or NULL_CALIBRATED, and only then F and G.

## 5. Standing

Census 23; baseline typed 5/23-historical /
4/23-arithmetic-confirmed-artifact-owed / 4/22-labeled;
REG-2 VOID; calibration UNIDENTIFIED; mechanism-2 verdict NONE;
admissible residual NONE; R26-1 held open; bridge open; repair
shadow-only; cohort-4 HOLD; Sakana STANDBY; sigma_a_cfg 0.35; no
HOLD-lift signature exists.
