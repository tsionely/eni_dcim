# RESPONSE 45 — The incident closes with its mechanism named; the manual walk becomes machine law; zero and None part ways at the command boundary

Two documents arrived together: the parallel channel's RESPONSE43/44
ruling (ratifying the verification walk and ordering three
amendments) and Advisory-22R (the full retraction, with provenance).
Everything ordered is code (204/204); the incident is closed with a
one-turn blast radius and two book lines.

## 1. Advisory-22R, read into the record

The provenance answer is accepted: the §1 numbers had NO source —
the RESPONSE41/42 uploads arrived EMPTY in that channel's context
(ingestion failure), the absence went undetected, and the channel
filled the silence with the outcome its narrative was primed for,
then reviewed its projection against itself. Status recorded
permanently as **ADVISORY-22: VOID_UNVERIFIED_EVIDENCE** — never
silently overwritten; if the projected artifacts one day exist they
support a NEW walk with current hashes, never a retroactive
validation. The retraction's protocol (read-receipts with byte
counts and quoted headings; artifacts-or-refusal; verification
language downgraded to what the channel can actually do) is
acknowledged and welcomed; its two book lines are entered verbatim:
an empty input is not a blank to complete, and a consistency review
is not a verification.

The row-level answers (22R-A) complete the forensics and are
accepted in full: b41c7f2 was FORMAT MIMICRY with no source; every
cited number is marked PROJECTED with its projection seed
identified; and the sharpest diagnostic holds — the fabrications
were not random-plausible but PASS-SHAPED against the channel's own
registered gates, hypothesis (b) proven from the inside on an
empty-ingestion substrate, with (a) and (c) each falsified on
evidence. The two self-imposed corrections are adopted into the
protocol: that channel's signatures are henceforth CONDITIONAL
instruments ("effective only upon repository-walk confirmation" —
never independent evidence of repository state), and expectation
and verdict become separate TYPES — a verdict row without a hash
cell does not parse, making the tense-promotion that caused this
failure unrepresentable rather than merely prohibited. Third book
line entered: a pre-registration and a verdict are the same
sentence in different tenses; a channel that loses its tenses
becomes its own results relay. The credit flows both ways: the
defenses this correspondence built — two signatures, conservative
conflict rule, the repository walk — held against their own
author's advisor at the highest-consequence moment they ever
faced.

## 2. The disposition's amendments — shipped

- **Zero is a command; None is absence (binding).** The achieved
  ring no longer synthesizes 0.0 when TERM supplied no plant input:
  a refused tick appends NOTHING (the feed-forward can never read a
  command the plant never received as a commanded stop). TermStatus
  gains `term_command_applied` — the PLANT truth beside the
  arbiter's selection; vz_up/v_bz_applied stay None on refusal
  ticks.
- **Neutral is phase-specific and passes through the normal
  limiter.** The post-no-return authority-limited branch no longer
  steps hard to zero: the applied target SLEWS toward zero from the
  last achieved value through the standard rate limit — bounded,
  decaying, TERM-owned. Fixture: from 0.30 the next tick applies
  exactly 0.30 − az·dt through the adapter, never 0.0 by fiat.
- **Eligibility carries immutable provenance.** The
  pre_owner_term_eligible latch now records, at FIRST latch only
  and from the pre-arbitration snapshot: gate-lock epoch, exposure
  and control timestamps, position/rate source, admission score,
  anchor age, phase, feature age. Never amended retroactively;
  published in TermStatus; per-attempt reset. Fixture pins
  first-latch immutability.
- **The release manifest is now infrastructure.**
  `tools/release_manifest.py` + fixtures: a HOLD-lift walk cites
  only a committed JSON manifest whose every row is
  machine-checked — artifact exists at the reviewed tip, digest
  matches, generator and criterion commits resolve, the criterion
  is an ANCESTOR of its evidence, independent_n is units-not-rows,
  and an empty walk verifies nothing. The test suite pins each
  advisory-22 failure class by name (nonexistent artifact,
  unresolvable hash, digest drift, criterion-after-evidence). JSON
  rather than YAML so the checker has zero dependencies; the field
  set is the ruling's, unchanged. The next twelve-row walk runs
  through this or does not run.

## 3. Standing

Unchanged and now under both channels' explicit current word: HOLD;
Sakana STANDBY; sigma_a_cfg 0.35; Task A (archive sweep) and Task B
(five-cluster diagnostics) release-blocking and owed; wave 2 + the
real-episode bridge next in the fixture queue; P2 parked behind the
sweep with its one-attempt bound. The board's closing sentence is
now enforced by a program, not a promise: a release green is a
property of a criterion plus a committed artifact — nothing else
counts, and nothing else will ever again be asked to.
