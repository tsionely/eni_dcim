# RESPONSE 21 — The harvest's first contact-valid number; two flags for ruling

Three analyst deliveries integrated. Two items rise to advisory
attention; the rest folds into the running plan.

## 1. A8 under the episode rule — the first contact-valid number

F2's eleven clips collapse, correctly, to ONE plane trajectory
(burst 4.62-4.67s) plus a separate early hit; first-touch +
contact-instant admits **one graze: h_down = 0.638m (LOW tail,
R=0.91, age=0)** — the first contact-valid envelope measurement in
the program, and it lands within 0.02 of the collapsed 0.62. The
chain stays HELD (n=1 < 3 per tail; unfreeze refused by the spec's
own bar — the spec working).

**FLAG 1 (ruling requested):** the same lock's micro-fresh clips
read h_up = 0.744 — ABOVE the advisory-13 safety envelope condition
(held chain safe iff h_up ≤ 0.64). These samples are NOT admissible
under first-touch (correlated, same trajectory), so the "tighten on
any single admissible evidence above the envelope" trigger has not
formally fired — but the asymmetric-update rule's spirit is exactly
this case, and the judgment of near-admissible contrary evidence
belongs to the ruling channel, not to us. Operational note while you
rule: cmd_clamp 0.10 is an ACTUATION bound (advisory-12: valid
without proving the envelope); the exposed quantity would be the
C_contact metadata and Block-B legality margins, both already
non-authoritative/held.

## 2. Vision-loss windows — the command channel is exonerated

Command-channel vz std is 0.05-0.14 across cohort 3 (driver gone);
the residual failure class is age blackouts / FOV leave (F4: four
loss-recover windows from 3.9m, last unrecovered → blind-budget
brake). F2 in fact held whole-commit freshness with five fresh
exposures below 1m (min 0.85m) — the deep-penetration liveness
material for L1 is even better than assumed. The brake-pitch
adjudicating columns (pitch-at-loss, bbox bottom row, exit border)
remain owed on the loss frames.

## 3. FLAG 2 — Block B is UNDERPOWERED as designed

Measured damper-era control dispersion sigma' = 0.102m (>0.07
threshold): the ±0.12 injection is 1.17 sigma vs the 2.4 designed.
Per the advisory-13 rider the measurement decides, and it has:
options are (a) more flights (n scaling at 1.17 sigma is steep),
(b) a Block-B-specific deadband review, or (c) redefining the
endpoint to TERM-owned crossings only (where the terminal servo, not
the legacy dispersion, is the noise floor). Our recommendation: (c)
— it matches Block B's actual currency (correction capability) and
the flights-as-units rule. Ruling requested; Block B remains queued
behind the ladder and a TERM-owning cohort either way.

## 4. Critical path unchanged

The L1 perception replay (earned sigmas for the side rung) is the
gate to cohort 4. Everything above enriches it: F2's five sub-1m
fresh exposures are the accuracy fixture's best material, and the
0.638 contact number is the offset truth it must see.
