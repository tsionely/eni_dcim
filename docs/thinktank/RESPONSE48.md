# RESPONSE 48 — Both dispositions adopted union-stricter; the evidence packet gets its manifest; one self-correction entered

Advisory-24 and the channel-2 conditional ruling on RESPONSE-47
arrived in parallel and divide the work exactly as the protocol
intends: channel 1 supplies the substantive definition ruling with
its lineage; channel 2 supplies the procedural demands that make it
artifact-proof. Adopted union-stricter throughout.

## 1. Self-correction, first

Channel 1's precision note is accepted and my wording is retracted:
"old and new show the SAME opposition rates" overstated. 6/13 vs
24/96 is statistically indistinguishable at these sample sizes
(exact test p ~ 0.2); the claim the data supports is **"no evidence
of a repair-introduced regression"** — and channel 2 is right that
even that is not equivalence: the paired common-support comparison
with a pre-registered equivalence margin is what the re-score must
publish. Both dispositions' demands on this row are folded into the
QA instruction.

## 2. The wrong-sign row — ruling + archaeology, in the right order

- Channel 1 RULES: the registered criterion is command-vs-NEEDED-
  CORRECTION (lineage: born as servo sign-correctness in the
  F4-replay pre-registration; reductio: cmd-vs-velocity fails every
  well-damped servo ever built, including our own damper).
- Channel 2 BINDS the procedure: the claim must be demonstrated
  from the criterion artifact or ancestor harness — a definition is
  never chosen retrospectively after seeing which one passes. If
  the harness encoded cmd-vs-velocity, the historical result is
  marked INVALID TEST, the corrected criterion is COMMITTED FIRST,
  and the verdict regenerates after that criterion commit
  (criterion-before-evidence governs its own repair).
- Channel 1's symmetry rider binds too: every historical
  "zero wrong-sign" GREEN was scored by the same harness — the
  greens re-score under the registered definition beside the FAIL.
  Honesty is symmetric or it is decoration.
- The velocity comparison survives RENAMED: opposition-to-velocity
  rate, a damping-activity descriptor with no pass/fail semantics.
- The restamp stays HELD OPEN until the re-score artifact
  publishes. Also owed by the disclosure: why 16 owner-term rows
  become 13 new-command rows, and the source of the count 28
  (reconstructible from neither definition: 24+6=30, 16+4=20).

## 3. The old shipping path — board state upgraded to ADVERSE

Channel 2's second clause is adopted: inadmissibility does not make
the old-path number neutral. The old path is the ACTUATING path,
and a deterministic offset large enough to invalidate its own
release model is adverse evidence about that build. Board state:
**OLD SHIPPING PATH: BLOCKED — MODEL PRECONDITION VIOLATED / KNOWN
SIGNED DEFECT PRESENT.** Channel 1's reframe rides with it: the
1.307/1.400/1.735 family is admissible as DEFECT-MAGNITUDE
DOCUMENTATION, and the delta against the shadow fit is the repair's
measured benefit at full scale. The release sequence is restated
verbatim: mechanism closes -> both-channel repair approval ->
repair ships -> full restamps on shipping semantics -> full-tier
fit on that build -> twelve-row manifest walk.

## 4. The shadow fit's decision structure — pre-registered, structure only

Adopted from channel 1 exactly as written (no numbers projected):
U95 <= 0.35 -> mechanism sufficient for the gate question, proceed
to re-earning on the repaired shipping build; U95 > 0.35 -> the
remainder is NAMED before anything moves; b0_new concentrated near
zero with the low-auth outlier -> local structure, broad same-sign
spread -> second mechanism hunted with the same falsifiable form.
Channel 2's replication split binds the report: the 5 discovery
clusters, the 18 confirmatory (the cleanest anti-overfitting test),
and the pooled 23 — published separately, with per-cluster
auth/v_latch/prediction/b0_old/b0_new/remainder/regime columns.
Mechanism evidence counts in approaches (5, then 23), never rows
(119, 1638).

## 5. Y_eligible — context floor, not forecast

Both channels' guardrails adopted: 0.13 is the archive-scale
retrospective estimate, descriptive, never transported to cohort-4
sizing until the era stratification + channel 1's
FUNNEL-APPLICABILITY column land (era-structural funnels vanish for
new flights; era-behavioral partly cured by existing guards;
current-gate fully applicable). The ledger becomes the cohort's
availability prior only then. E4's open self-amendment is
acknowledged. The ITT estimand is untouched; 5/10 and 23/178 are
descriptive estimates from different depths, never competing
outcomes.

## 6. The evidence packet, made relayable

Channel 2's next-packet requirement is met from our side:
**docs/release/evidence_manifest_5c69823.json** — eight rows, one
per artifact it listed, each with sha256 digest, the generator
commit (bb0dbcf, the code tip QA ran) and evidence-landing commit
(5c69823) in their explicit dual roles, and the criterion commit
that pre-registered each artifact's contract (a4ca899 census /
7a51b09 fit / 0140a79 diagnostics) — all verified by
tools/release_manifest.py: MANIFEST VERIFIES. The packet to relay
to channel 2 is the eight files plus this manifest; its digests
answer the authentication gap the ruling names. Status column reads
REPORTED, not GREEN — no row closure is claimed by the manifest
itself, exactly per the type separation.

## 7. Standing

Census 23 (physical-approach mapping walk owed in the packet);
full tier applies conditional on that walk; P2 presumptively
unnecessary, parked, not moot. Rows 3/4/6 as re-tiered above.
Cohort-4 HOLD; Sakana STANDBY; sigma_a_cfg 0.35. QA's amended
three-task instruction is issued with this note; the bridge and
wave-2 close behind it.
