# RESPONSE to ADVISORY 4 — certificate adopted, one ask already answered

Paste back to think-tank #1.

## 1. Dispositions

- **C1 certificate + three-state machine: ADOPTED as the tracker's
  identity stage, to be implemented as specified** — inheritance from
  the last fully-gated quad, the five invariants, PROBATION quarantined
  to tracking/rate-only, the terminal rule (no fresh CERTIFIED below
  ~1.4m). Implementation starts when tank #2's round-4 answer (the
  override-arbitration adapter) lands, so the certificate and the servo
  wiring ship as one auditable work item. C3 accepted as the OFFLINE
  auditor that labels fixture ground truth; C4 held as last resort.
- **c_i as a filter state: ADOPTED** — trust-is-variance unifies the
  certificate and the anchoring machinery; the model-speed anchor
  (Ż_model × τ_visual) enters as the always-on floor. Your
  failure-signature analysis (constant multiplicative range bias with
  t_c exactly correct; divergence of t_c-scheduled vs range-scheduled
  events as the live diagnostic) is recorded in the T1 design notes and
  the four-configuration kill test is adopted verbatim.
- **§0 budget correction accepted**: the warm vertical number breaks
  the Advisory-2 budget, and your framing is fair — insurance → veto →
  top-up is three open-loop patches on one axis. The patch line ends
  there: no further vertical compensation tuning; the axis closes only
  via the certificate-gated servo (+ ribbon if R1 clears it).

## 2. Asks — status

- **A3 (banner height): ALREADY MEASURED** — you may have missed the
  RESPONSE5 addendum: the banner BOTTOM edge sits **+0.15m above the
  opening center** (measured at R≈7.5m from a trusted fix; the first
  Test A failure was exactly the d*=0.8-vs-banner confusion). So the
  banner-bottom border-exit ping can be association-gated now; note the
  banner is WIDER than the gate (~2.0m by the same measurement), which
  helps separate its exit signature from the top bar's.
- **A2/R1: re-flagged top priority to the analyst**, on ALL committed
  takeoff→end slices (five close approaches across phase5c/5d,
  including both LOW and HIGH arrivals — the row-offset distribution
  will show the ~+30px-above-horizon signature you predict if the
  geometry holds).
- **A1**: the three adversarial segments are being labeled as the
  standing FA=0 suite (frame-id manifests per slice).
- **A4, A5**: assigned (bar width; minimum inter-gate spacing).

## 3. Flight-state note for your ledger

phase5d (top-up build) flew: the ceiling-chase class is gone from the
believed/lock behavior (no far-fiction, no next-gate steal, active gate
stable), residual short ceiling excursions remain post-retreat; terminal
arrivals were HIGH/LOW/LOW — confirming your §0/§5 point that the axis
is un-tunable open-loop. Vision starvation in the final ~2m was
A/B-verified NOT to be our plausibility gates (fixes-produced nearly
identical with gates off; the extra fixes were lock-rejected fiction) —
it is the structural frame-exit, which is precisely the regime your
certificate + the ribbon loop are for.
