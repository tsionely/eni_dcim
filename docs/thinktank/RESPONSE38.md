# RESPONSE 38 — The two channels compose: the archive sweep hunts the sixth cluster while the five legal ones go to work

The RESPONSE36 disposition and Advisory-20B arrived in parallel and
compose without conflict: one channel asks whether the sixth cluster
is already on disk (the full-archive retroactive sweep, tasked in
RESPONSE-37); the other rules that the five legal clusters must not
sit idle while anyone waits. Both adopted in full. Nothing in either
ruling changes flight code this round — it is scheduling, contracts,
and reporting, all forwarded to the instruments.

## 1. Adopted verbatim

- **Collection stop ratified on the outcome; the mechanisms stay
  distinct.** f1 (correct safety rejection, permanent fixture) and
  f4 (unexplained pending autopsy) are equivalent ONLY for the
  stop decision — never pooled as one perception failure class.
- **Y_eligible** joins the lift package: legal-transition approaches
  over total metrology approaches — the eligibility/liveness
  denominator that keeps a precise conditional drift model from
  implying frequent TERM eligibility. It composes with 20B's
  coverage-censoring ledger (per-label rates) as one context
  artifact.
- **No gate weakening to manufacture the sixth cluster**; failed
  approaches never enter sigma_a in any form (not zero-residual,
  not censored, not negative examples). The eligibility denominator
  is where they live.
- **f4 autopsy contract** forwarded to the analyst as ruled: the
  per-exposure stage table (detector candidate -> pose -> identity
  -> scale gate -> certificate state/transition -> e_z extraction
  -> producer armed -> SIDE mode/cert -> border/blur/bloom ->
  feature age), stop at the first stage diverging from f2/f3 and
  the legal archive approaches, classify into branches A-E. The
  general-repair rule (named mechanism, no flight-ID conditioning,
  replay-wide rerun, honest re-adjudication, fixtures green) is the
  same standard that kept f1 out.
- **Profile amendments ranked, not selected** — earlier producer
  arming (R_arm from the recorded overlap/loss distribution,
  reviewed common-arm change with the full re-run list) over
  certify-then-approach staging over perception repair; the autopsy
  picks the branch. Gate loosening prohibited. Parked until the
  autopsy AND the archive sweep report — if the farm is on disk,
  flight 5 may never need designing.
- **R26-3 contract** forwarded to QA: the full per-interval column
  list, the eight-regime matrix, the evidence hierarchy (real
  TERM-owned recordings for ordinary corrections; closed-loop
  simulator fixtures for saturation/authority-limited/constructed
  triangulars — the simulated vehicle must physically respond;
  shadow commands are never physical data; nothing dangerous is
  provoked in real flight), and the green criteria including
  NOT-COVERED visibility for missing regimes.

## 2. The new GO: five-cluster diagnostics start now

Tasked to QA alongside the archive sweep, explicitly DIAGNOSTIC
(may falsify the repair, may expose wrong sign or liveness loss;
may not lift HOLD, declare a validated age, or release sigma_a):

    old policy-attenuated vs unattenuated anchor, per cluster
    delta_latch mechanism table (b0 vs -(1-auth)*v_latch)
    five-cluster signed b0/b1 diagnostics + regime invariance
    pseudo-transition floor diagnostics
    shadow R26-1 liveness restamp (dual-read columns)
    command-step and admission comparison old vs new

The ruling's logic is adopted as the plan's spine: a mechanism that
fails across five legal approaches dies BEFORE another cluster is
collected; a clean five-cluster result turns the eventual sixth
into evidence-completion rather than a first look.

## 3. Board — re-tiered as ruled

GREEN/CLOSED: P4(d) + negative controls, SIDE-cannot-corrupt,
dual-read purity/algebra, phase-wire blast radius (fixture
retained, off the critical path).
ADVANCE NOW (diagnostic, cannot close release): rows 3/4/5/7
via the five-cluster suite; 10b (R26-3 inventory -> simulator gap
list).
TRULY BLOCKED on the sixth independent approach: rows 6/8/9/10a.
Census 5; release fit not authorized; collection stopped; Sakana
STANDBY; flight 5 same-profile NO-GO. The archive sweep is the
cheapest path to unblocking the last tier. Cohort-4 HOLD.
