# RESPONSE 44 — The RESPONSE41/42 disposition adopted in full; three builds shipped the same round (200/200)

The parallel channel's joint ruling arrived carrying HOLD — which,
read beside Advisory-22, resolved the gravest channel conflict of
the program in the conservative direction (the verification walk is
RESPONSE-43; no release was relayed). This note records the
ruling's adoption and the three build items it ordered, all shipped
and fixtured.

## 1. Adopted verbatim

- **Retrospective vs online, named correctly.** The compound
  censoring signature is a valid retrospective mechanism label;
  "never re-certifies <= 3.5 m" is knowable only after the band is
  traversed and is partly outcome-definition — ONLINE USE
  PROHIBITED, 4.5 m as a runtime trigger PROHIBITED. P2's online
  trigger stays freshness-based exactly as registered (fresh
  certified FULL stale + not yet in the harvest band + identity not
  revoked). The success criterion is the EXACT legal cluster
  predicate — re-detecting FULL three times does not count; the
  failure criterion ends the one permitted attempt cleanly.
- **The table freezes before the sweep.** No retuning of 4.5/3.5
  after seeing the archive; the sweep publishes the 2x2 by era, P1
  pass count, exact legal count, closest-FULL distribution,
  mechanism labels, Y_eligible by era, age-bin contribution of
  every legal approach. If separation fails out-of-era, the
  failures stay visible and P2 re-adjudicates from the mechanism
  table, never from threshold tuning.
- **Y_eligible = 0.50 accepted as descriptive** with its Wilson
  interval (0.24-0.76) attached — never a mission prior, never a
  power parameter, never an exclusion license. **Cohort-4 primary
  analysis is intention-to-treat**: all assigned approaches stay in
  the primary outcome; eligibility strata are secondary process
  metrics; no "TERM effect among the eventually-eligible" —
  post-treatment conditioning could select favorable live-arm
  paths.
- **R26-3S wave 1 GREEN; wave 2 GO** under the clause-specific kill
  rules (triangular sign/order per leg with feed-forward returning
  exactly to anchor after the registered slew path; saturation
  consuming achieved-only; owner/phase stability; the asymmetric
  bridge — disagreement revokes the clause, agreement earns one
  real-domain consistency fixture and nothing more). The
  wave-2-may-close / may-not-close lists are adopted as the
  execution spec.

## 2. The three ordered builds — shipped this round

1. **§6 authority-limited reversibility split** (flight code): the
   tilt-envelope branch now follows the SAME split as the age
   ceiling. Pre-no-return: TERM does not apply, the hold/abort
   request raises (planner's band+freshness guards decide
   feasibility), and the achieved thread returns None — so
   note_applied records 0.0, and a command that never reached the
   plant can never later be read as a real rate change by the
   feed-forward. Post-no-return: TERM REMAINS the applied owner
   with a neutral command — the previous behavior (legacy keeps
   the tick) was, as the ruling implies, a handback to believed
   altitude in disguise; it is gone. The request flag is now
   recomputed per tick (never lingers stale). Fixture: ownership
   persists, vertical never actuates pre-latch, flag raised,
   achieved thread None; post-latch neutral TERM-owned command,
   flag down.
2. **pre_owner_term_eligible** (telemetry): latches when the common
   observer satisfies the legal door while the owner is still ALT —
   before any TERM command exists — published in TermStatus,
   cleared per attempt. Fixture: latch observed on an ALT tick of a
   capturing approach; admission-blocked approach never sets it;
   reset clears. The ITT analysis reads THIS marker.
3. **§5 permanent calibration invariant** (fixture): the mock rig's
   calibration enters both command arms identically (parametrized
   rigs both capture and close in both directions), and the REAL
   rig's production trim literal is proven absent from the mock
   module by source scan — changing the real constant cannot change
   any mock result. (The scan initially tripped on its own banned
   literal; it now constructs the string it forbids. Small, but the
   kind of small the book keeps.)

## 3. Standing

Critical path unchanged: Task A (archive sweep) + Task B
(five-cluster diagnostics) owed by QA; wave 2 + bridge next in the
fixture queue; P2 parked; one staged attempt maximum; Sakana
STANDBY; sigma_a_cfg 0.35; cohort-4 HOLD — now under BOTH channels'
current word, pending Advisory-22's channel locating the source of
its §1 numbers.
