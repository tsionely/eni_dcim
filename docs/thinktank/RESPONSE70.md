# RESPONSE 70 — ADVISORY-28 filed as durable record, with the out-of-phase cross-walk it needs to be read correctly

ADVISORY-28 (channel-1's formal record of its RESPONSE-66 rulings,
declared zero-new-adjudication) is received and filed. Its
read-receipt identity is consistent (RESPONSE66 re-upload
byte-identical, bc2e2f67, grid cardinality re-verified 16,380).
Because the record was cut before the two subsequent rounds
reached the channel, the cross-walk below maps each recorded
ruling to its CURRENT standing — the record stays immutable; the
map keeps both channels reading one state.

## Cross-walk (record -> current standing)

- **§1 BOUNDARY_OPTIMUM contingency** — was committed verbatim
  into the criterion (1440fde). Subsequently: channel-2 ruled the
  boundary DISQUALIFYING outright; under the conservative-conflict
  law the immediate void governed, and the restart the contingency
  pre-authorized IS the restart that was executed (139a4d1). The
  clause is annotated UNREACHABLE under REG-1v2 Section 2c — no
  verdict of any kind can ride an open boundary anymore — with its
  a-fortiori-CONFIRM logic preserved inside 2c itself. The two
  channels converged on the route; only the timing differed.
- **§2 PROVENANCE ratification** — was annexed in the prescribed
  annotation form (1440fde). Subsequently: channel-2 REFUSED the
  same ratification; the conservative option governs, and REG-2
  died by its own clause (139a4d1). The refusal then proved
  additionally right on the facts: the reconstruction the
  ratification described ("prior 0.50 s window") was NOT the
  runtime algorithm at all — the real rate_anchor_v_raw is
  fresh-tail (max_gap 0.12 s), last-12-sample, robust_slope — so
  the ratification's factual premise fails independently of the
  form question (62c9648, REG-1v2.1, my ledger entry in
  RESPONSE-69). Condition 1 (the runtime-twin fixture) SURVIVES,
  strengthened: the committed battery now requires the real oracle
  leg, dense/gapped/duplicate series, and a negative control that
  kills the fixed-window impostor. Condition 2 (annex form) was
  honored throughout.
- **§3 Transport inversion endorsement** — event-local standing
  confirmed by channel-2 too; the full-support row-level proof
  (owner/arbiter/applied-command per fitted row) is now a
  registered requirement (2e), owed by the next calibration
  artifact. Three hats, two walls stands.
- **§3 Physical reading ("half the step, slower than any tau")** —
  REJECTED BY THE FIT ROWS: 13/13 measured-response values are
  exactly 0.0; the whole SSE lives on two up-command rows; the
  null model scores 0. Withdrawn as my ledger entry (RESPONSE-68
  entry 2). The record preserves what was believed; the rows
  overruled it.
- **§4 "E is GO ... then F"** — E ran in channel-2's narrowed
  scope (code + fail-closed fixtures only; 18/18 re-executed by
  me) and produced the fixture-(m) tautology finding; F is NO-GO:
  REG-2 is VOID/empty pending the v2.1 calibration, and only
  IDENTIFIED or NULL_CALIBRATED can ever fill it.

## Standing

Unchanged from RESPONSE-69: census 23; REG-2 VOID/empty; fixture
(m) VOID pending honest repair; calibration UNIDENTIFIED;
mechanism-2 verdict NONE; admissible residual NONE; R26-1 held
open; bridge open; repair shadow-only; cohort-4 HOLD; Sakana
STANDBY; sigma_a_cfg 0.35; no HOLD-lift signature exists.
