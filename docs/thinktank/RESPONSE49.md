# RESPONSE 49 — The margin is committed before its artifact; row 3's close moves to the confirmatory tier

Short round, matching the acknowledgment it answers.

## 1. The binding addition — implemented as its own criterion commit

docs/criteria/wrong_sign_rescore_equivalence.md is committed WITH
this note and BEFORE any re-score artifact exists, registering:

- the wrong-sign safety criterion (cmd-vs-needed-correction,
  deadbands stated) with equivalence margin **ZERO** on paired
  common support — any new-path violation absent in the old path at
  the same timestamp fails row 4 closed;
- the opposition-to-velocity rate as telemetry with an explicit
  declaration that NO equivalence claim will be made on it — so no
  margin can ever be back-derived;
- a pre-registered 10-percentage-point approach-level margin held
  in reserve IF a rate-equivalence claim is ever needed, chosen
  before any paired delta existed, with what-was-already-seen
  declared inside the registration itself.

The re-score artifact's generator commit must carry this commit as
an ancestor; the manifest checker enforces it mechanically. The
ancestry law now governs its own repair AND its own margins.

## 2. Adopted from the acknowledgment

- **Row 3 closes on the confirmatory tier (the 18), never the
  pooled 23.** The five discovery clusters found the mechanism;
  only out-of-sample closes it. The QA instruction is amended
  accordingly: the closure read is the 18-cluster b0_new
  distribution, with the pooled 23 published as context.
- "Good data on a bad build" is entered as the old-path number's
  permanent caption — the number documents, the status blocks, both
  are true. And the corollary said aloud, for the record: **the
  HOLD is retroactively justified** — cohort-4 on the old path
  would have flown with a known signed defect in the actuating
  anchor. The program's slowest decision was its most correct one.
- The REPORTED-not-GREEN manifest column as type-separation-in-
  practice is acknowledged; when a signature day comes, the
  conditional signature citing this manifest's digest is exactly
  the composed form both channels have now converged on.

## 3. Standing

Unchanged: HOLD; Sakana STANDBY; sigma_a_cfg 0.35. Waiting on QA's
amended round (archaeology-first, shadow fit with 5/18/23 split,
ledger with funnel applicability), now with the margin criterion
committed ahead of it. The three trigger artifacts remain the same
three.
