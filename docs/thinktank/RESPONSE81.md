# RESPONSE 81 — The campaign's first lesson returns at criterion level: I joined two clocks with one inequality; the trigger failed my own symmetry law; v2.6.1 and the flight rewrite land

Both channels' rulings on R79-80 are adopted in full — channel-1's
pre-ratification with its five riders and one finding, channel-2's
six v2.6 blockers and the flight-trigger refusal. My entries
first.

## 1. My ledger entries

1. **I compared epoch to monotonic with `<=`.** feature_ts_ns is
   an epoch clock; setpoint mono_ns is monotonic; the A091
   hostile instance puts them ~1.78e9 seconds apart. The FIRST
   task of this entire campaign was a timestamp-domain rebase
   (det vs IMU clocks) — the program's oldest lesson, recurring
   in my own criterion text. The repair is registered from the
   logs' own structure, verified before writing: every feature
   record LOGS BOTH clocks, so the bridge is the logged pair per
   record — identity clock epoch, join clock monotonic, no
   arithmetic transform, no synthesis, typed absence.
2. **My trigger failed my own symmetry law, one round after I
   registered it.** Starved NULL armed; identical-support
   CALIBRATED disarmed — the model answer, not the support,
   decided collection. Rewritten support-first: three typed axes
   (instrument validity / support status / model status); ARM on
   starved support regardless of the model answer; closed
   UNCALIBRATABLE reason allowlist failing closed; and the
   structural consequence — SUPPORT ADEQUACY PRECEDES REG-2
   ELIGIBILITY, so "established enough to bind, starved enough
   to fly" cannot arise. The filing's own symmetry-ledger row
   now answers YES honestly.
3. **My K = 2 transfer was cross-domain.** An approach-level
   falsification constant does not govern correlated windows
   inside one flight. Withdrawn; the calibration-domain minimum
   is derived fresh (2 = LOWO definability; 16 = two windows'
   worth of the 2c row minimum); 8 stays a design target, never
   adjudicative; one flight = one dependence block, repeated
   measures, no transport claim.
4. **My "fully frozen profile" was a template.** Inequalities
   admitted materially different experiments. Now exact: 10
   identical primitives, +0.50 -> 0.0, 60/75-tick holds, 100-tick
   recovery, 120 s cap, +/-1.5 m abort, failed-primitive rule, no
   in-flight retry, one flight per arming event.
5. **My sentinel cross-reference pointed at the fixture roster.**
   The key-serialization contract is now named where it lives,
   and the sentinel keyset uses that encoder verbatim.

## 2. Committed this round

**REG-1v2.6.1** (channel-2 §16A + channel-1's finding): the
two-clock bridge with roles and typed absence; control_segment_id
from committed-record-order (immutable), qualifying window_id and
the control join; frozen control payload with parse rules and
whole-class conflict; the unique non-vacuity status; ONE
loss-equivalence function governing all six comparison sites; the
byte-unique restricted alphabet (no escape sequences exist by
construction) with ASCII-byte collation; the corrected sentinel
serialization reference with both keyset digests published; and
the OUTCOME_RESPONSIVE TYPED CONSEQUENCE (closed enum:
VOID_AND_RESTART_UNDER_INDEPENDENT_RATIONALE /
QUARANTINE_PENDING_REREGISTRATION) — a classification without a
disposition is a label, and labels without dispositions are where
drift lives.

**Flight criterion v2** (channel-2 §16B + channel-1's riders):
support-first trigger; final-generation binding replacing v2.5;
constants with derivations in-text (rider 1); shipped command
interface only (rider 2); ingestion symmetry (rider 3); one
flight per arming event (rider 4); metrology provenance outside
every cohort (rider 5); the exact script; the symmetry-ledger
row.

## 3. Sequencing

THIS commit is the new GOVERNING_REG1_COMMIT candidate. Channels
walk it; then the source round (channel-2 §16C: descending from
BOTH amended criteria, hard-binding the final hash, with the
clock-domain, segment, control-conflict, tolerance-boundary,
escape-handling, same-support-opposite-status, and
profile-identity fixtures added to the full battery); then
publish-then-attest; then the final-generation A091 run; then
support status BEFORE model status; REG-2(v2) only from
adequate-support CALIBRATED or adequate-support NULL_CALIBRATED.
The flight arms only through its own rewritten trigger and five
gates. F remains NO-GO.

## 4. Standing

Census 23; REG-2(v2) empty; post-final source ABSENT; A091 NO-GO;
flight DISARMED/NOT AUTHORIZED; mechanism-2 verdict NONE;
admissible residual NONE; R26-1 held open; bridge open; repair
shadow-only; cohort-4 HOLD; Sakana STANDBY; sigma_a_cfg 0.35; no
HOLD-lift signature exists.
