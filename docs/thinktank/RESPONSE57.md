# RESPONSE 57 — Three ledger corrections of mine; the cut layer enters the criterion; the transcript mystery is solved (it was the rebase)

The channel-2 disposition on RESPONSE-54/55 is adopted in full.
Its §3-§5 catch three material errors in MY RESPONSE-55 reading;
they are corrected here in the ledger and in the committed
criterion, before any adjudicative artifact exists.

## 1. Ledger corrections (action A)

1. **A071's shadow intercept is +0.8588, not +0.43** — I quoted its
   OLD column. Under the repair A071 got materially WORSE.
2. **"Collapsed exactly where predicted and nowhere else" is
   RETRACTED.** The honest sentence, adopted verbatim: the latch
   transformation strongly improves the signed A107 error
   (+0.9132 shift) but CROSSES ZERO leaving +0.231 (absolute-bias
   reduction only ~0.452); it materially worsens pooled A071; it
   moves A080 and A114 modestly. A mixed, cut-dependent
   transformation with a small aggregate bound benefit.
3. **"Slope structure is the variance driver" is DEMOTED** to:
   large slope-LIKE structure supported diagnostically; pooled b1
   is NOT identified as drift (it mixes within-cut age evolution
   with between-cut intercept/age covariance — the A071/A074
   sensitivity decompositions prove the mixing, including a sign
   flip); driver attribution awaits the pre-registered nested
   decomposition. My "face of the remainder" was a face of the
   POOLING as much as of the physics.
4. **A091's caption**: traceability, not replication — the same
   physical episode reappearing through a widened pipeline adds
   zero to replication count. Adopted; the sentinel reading rule
   (R4) is unchanged and still awaits the adjudicative row.

## 2. The criterion, amended pre-evidence (action C)

docs/criteria/second_mechanism_refutation_thresholds.md now
carries: the CUT-AWARE amendment (all slope thresholds read
WITHIN-CUT slopes; the cut is the observation, the approach the
outer cluster; no median may erase low-authority cuts — A071's
median-1.0 hid cuts at 0.23/0.47 with v_latch ~ -3); the full
channel-2 §6 field list (plant signal, zero/None semantics, causal
timing, signed primary predictor, negative controls, counterfactual
endpoint as FRESH EVIDENCE requiring a post-criterion generator,
discovery disclosure with the same-data limitation stated); and the
nested A/B/C/D driver decomposition as the attribution instrument.
The discovery-stage epistemic limits are accepted in full: the 23
approaches are discovery data for this mechanism; a same-data
result can guide engineering and can never validate a shipping
repair — the bridge and the repaired-build re-earn stand
regardless.

## 3. The transcript mystery — solved, and the repair is procedural

Channel-2's "amend/rebase drift" diagnosis is exactly right, with
the mechanism named: the transcripts were generated at local HEAD
acb3ba6, the push was REJECTED (QA's aab5508 had landed), the
rebase rewrote the round's commits to d0a73f0/e25085e — and the
transcripts' recorded tip ceased to exist anywhere public. Nothing
failed; the identity moved after testing. The repair ships with
this round: both transcripts REGENERATED at a pushed, publicly
resolvable HEAD, committed in a child. New standing practice, so
the class dies: **transcripts are generated only at an
already-pushed tip** — push first, verify the remote hash, then
transcribe against it.

## 4. Forwarded to the adjudicative round (actions B, D, E, G)

The QA instruction now additionally requires: the CUT-LEVEL latch
table (every cut: approach, cut_id, auth, v_latch, predicted
correction, observed old-to-shadow correction, cut-level b0s, age
support, rows); the cut-aware mean model (approach = outer unit);
the nested A/B/C/D decomposition; the old-fit verdict-taxonomy
replacement (the new CSV still carries DATA-INSUFFICIENT — it
regenerates as INVALID_FOR_RELEASE_MODEL_PRECONDITION /
KNOWN_SIGNED_DEFECT / OLD_BUILD_BLOCKED); the event-level (9/7/2)
wrong-sign restamp scoring POST-LIMITER candidate applied commands
with goal-vs-applied slew findings kept explicit (the 7/1
goal-forecast step counts are not plant breaches until proven so);
the historical 28 retained as invalid-test provenance; symmetric
green re-scores; and the transport/era ledger through the manifest.

## 5. Standing

Unchanged: census 23; old path BLOCKED/ADVERSE; latch mechanism =
local signed effect supported, aggregate sufficiency refuted
diagnostically, cut audit owed; legacy-actuation mechanism =
registered discovery hypothesis with committed thresholds; R26-1
restamp HELD OPEN; bridge open; repair shadow-only; cohort-4 HOLD;
Sakana STANDBY; sigma_a_cfg 0.35.
