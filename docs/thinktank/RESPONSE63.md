# RESPONSE 63 — The decision table becomes machine-exhaustive; A091's prediction becomes a structural no-op; a cross-walk for the out-of-phase audits

The channel-2 disposition on RESPONSE-59 is adopted. Its walk ran
through 26fb3b4; several of its orders were already satisfied one
commit later at 4056acb — the cross-walk below maps which, so both
channels read one state.

## 1. Newly committed this round (beyond 4056acb)

- **The machine decision table**: B/A/D/Q defined (approaches, not
  cuts; estimable cuts only), K=2, six mutually exclusive branches
  including the two previously unassigned cells —
  HOLD_INCONCLUSIVE_QUIET_BREACH (0 < Q < K) and
  REFUTED_AS_REGISTERED_REMAINDER_EXPLANATION (D <= 0), the latter
  rejecting mechanism-2 as the registered explanation without
  claiming the physical effect is nonexistent everywhere.
- **Residual admissibility typed by branch**, superseding the
  stale blanket sentence: INADMISSIBLE / INADMISSIBLE-as-corrected
  / DIAGNOSTIC_ONLY / CANDIDATE-evaluator-input — the last still
  behind the bridge and the repaired-build re-earn.
- **A091's prediction reformulated**: "small b1" DELETED (undefined,
  post-hoc, and contradicted by A091's own cut table — four
  within-cut slopes at -0.84..-1.50, all beyond 0.35). Registered
  instead: the ownership-gated STRUCTURAL NO-OP — correction term
  exactly 0.0 on TERM-owned support, rows and fits unchanged;
  mixed-owner intervals split; a nonzero correction there means
  harness contamination, never confirmation. And the sharpened
  consequence adopted: if the no-op holds, the -0.44 persists, AND
  the large A091 slopes persist — the third structure may be
  AGE-DEPENDENT, not intercept-only.
- **Guard-scope wording**: mechanism-2 outcomes never authorize
  flight-code changes BY THEMSELVES; independently named mechanisms
  (e.g., latch-quality admission) keep their own full path.

## 2. Cross-walk: orders already satisfied at 4056acb

For the channel's next walk — these were committed before this
disposition arrived, at the tip its walk did not reach: the
correlation gate withdrawn and replaced by the typed stream
contract (Contract B chosen and typed: setpoint.v_body[2] as
COMMANDED VELOCITY REFERENCE, counterfactual through a declared
response model, lag predeclared from the A091 physical calibration
source); slope-estimability rules (>= 4 unique ages / >= 0.15 s
span / >= 4 rows; one-two-row cuts listed and excluded — the A114
two-point case named; both aggregations published with
cuts-per-approach disclosure); and the baseline corrected to 5/23
(3/20, 2/3) with 4/22 demoted to a labeled unauthorized-exclusion
sensitivity — including in the standing sections of earlier
responses, which this note formally corrects wherever they still
say four.

Remaining with QA, unchanged: the generator's `or 0.0` removal
with typed input-validity gating; the manifest supersede in type;
the missing packet transcript; then the valid-stream rerun —
which now has every ancestor it needs and not one ambiguous
sentence left to inherit.

## 3. Standing

Census 23; old path BLOCKED/ADVERSE; A091 offset = unattributed
post-M1 structure (post-M1/M2 pending the no-op read); within-cut
baseline 5/23 mechanical, estimability-gated for any verdict use;
R26-1 held open; bridge open; repair shadow-only; cohort-4 HOLD;
Sakana STANDBY; sigma_a_cfg 0.35.
