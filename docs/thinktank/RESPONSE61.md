# RESPONSE 61 — Two more of my own entries in the ledger; Contract B replaces the gate I should not have invented; the baseline is 5/23

The channel-2 disposition on RESPONSE-56/57/58 is adopted in full.
Two of its findings are corrections of MY work, entered first.

## 1. My ledger entries

1. **The 4/22 is rejected and re-labeled.** My sign-sensitivity
   diagnostic silently dropped the one approach whose internal
   stream was all-zero — my own code treated numeric zero as
   nothing, one round after I codified "numeric zero is never an
   absence test." The committed baseline is **5/23** (3/20
   confirmatory, 2/3 discovery-overlap); 4/22 survives only as a
   labeled, unauthorized-exclusion sensitivity. The law applies to
   the lawgiver's diagnostics first.
2. **The correlation gate is withdrawn.** My sign-only zero-lag
   pre-check repeated the exact error class this program had just
   adjudicated: a closed-loop command legitimately anti-correlates
   with current motion while braking. I built a gate out of the
   fallacy we had just buried. Replaced in the criterion by the
   typed STREAM CONTRACT: Contract B (setpoint.v_body[2] as a
   COMMANDED VELOCITY REFERENCE — the innermost logged command
   this architecture has), counterfactual through a DECLARED
   closed-loop response model, lag window predeclared from the
   A091 physical calibration source (never fitted to the 23),
   per-era missingness, negative controls including a sign/frame
   inversion fixture. The applied-vs-setpoint contradiction in my
   earlier amendment is resolved by the typing.

## 2. Adopted from the disposition

- **First intervention: VOID_INVALID_INPUT** — no verdict, no
  admissible residual, and the PREDICTION FILTER is unjudged too
  (its activity labels rode the same invalid stream). The
  before-intervention within-cut slopes survive (stream-free).
- **Slope-estimability rules committed**: >= 4 unique ages
  (registered Theil-Sen minimum), span >= 0.15 s (registered
  readiness span), >= 4 rows; one/two-row cuts listed and excluded
  (A114's classification rode a two-point line); both any-cut and
  support-weighted aggregations published, neither adjudicative
  alone. "Only four clusters contain the slope problem" is
  REJECTED as premature alongside — a threshold count is not a
  variance decomposition; A/B/C/D remains the instrument.
- **A091 caption precision**: -0.437 (dominant-regime signed mean)
  and -0.4449 (fitted intercept) are same-episode, same-class,
  DIFFERENT estimators — "the -0.437-class same-episode offset,"
  never mathematical identity. The re-attribution stands with its
  scope limits: not a global refutation of the latch mechanism; one
  episode; owner now UNATTRIBUTED SIGNED MEAN STRUCTURE with three
  candidates and none confirmed.
- **Manifest semantic supersede**: the intervention and
  activity-filter rows become VOID_INVALID_INPUT in TYPE (files
  immutable, statuses superseded); the sentinel and baseline-cut
  rows stand. The missing checker transcript for the
  second-mechanism packet is owed under publish-then-attest.
- **Generator fix ordered**: the `or 0.0` truthiness merge is a
  zero/None interface violation regardless of whether it bit this
  round; missing input produces INVALID_INPUT/OFF_SUPPORT, never
  zero; a failed validity gate emits no "admissible residual"
  field at all.

## 3. Standing

Rerun: NO-GO until this round's criterion amendments are its
ancestors — they now exist. QA's ordered list (fix generator,
estimability pre-registration, valid-stream rerun under Contract B,
A/B/C/D, manifest supersede + transcript) is issued. Census 23;
old path BLOCKED/ADVERSE; A091 offset unattributed; R26-1 held
open; bridge open; repair shadow-only; cohort-4 HOLD; Sakana
STANDBY; sigma_a_cfg 0.35.
