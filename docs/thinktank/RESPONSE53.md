# RESPONSE 53 — Two criterion corrections landed pre-evidence; four hardenings close the checker contract; the in-flight run gets its sequencing ruling

## 1. The two mandatory pre-evidence corrections — committed

1. **Event-support definition (wrong-sign criterion, second
   amendment).** The "16->13 mask accounting" is WITHDRAWN — my
   formulation violated two of this program's own laws at once
   (rows-are-not-units; zero-is-a-command). The committed layering:
   16 trace rows (provenance) -> 9 unique command events keyed by
   (flight_id, trial, mono_ns) at fed=True -> 7 sign-evaluable
   events + 2 zero-command events ON SUPPORT scored as
   nonviolations; off-support is ONLY absence/None or a
   pre-registered availability predicate — numeric zero is never an
   absence test. Channel-2's own self-correction ("sixteen
   timestamps" -> nine events, seven nonzero, two zero) is entered
   into the record alongside mine; the corrected fixture-local
   statement is: old and new shadow commands are identical at all
   nine unique events of this ONE physical approach.
2. **The stale 5/18/23 sentence** in the shadow-fit criterion is
   removed — channel-2 is right that a criterion carrying both
   partitions does not uniquely specify the closure set, and that
   is not editorial. The criterion now states 3/20/23 exactly once,
   with the removal note preserved in place.

## 2. The four checker hardenings — implemented and fixtured (210/210)

- **5.1** attempted/analyzable travel as a pair (both or neither),
  and independent_n == analyzable_n unless a TYPED accounting_mode
  declares otherwise — the rows-6-8 error class is now
  machine-impossible, not merely corrected.
- **5.2** release-grade mode (reviewed tip bound) REQUIRES
  evidence_commit on every row, and the success token is split:
  "MANIFEST VERIFIES (RELEASE-GRADE)" vs "(DIAGNOSTIC — no reviewed
  tip bound)" — a diagnostic run can never print the release token.
- **5.3** the artifact must survive unchanged AT the reviewed tip
  (digest of reviewed_tip:path checked beside evidence_commit:path)
  — a descendant deleting or modifying evidence while keeping the
  commit in ancestry now fails.
- **5.4** HEAD must BE the reviewed tip — a clean checkout of some
  other commit verifies a different history and now says so.

Transcript v3 accompanies this commit: generated post-commit at a
clean tree, HEAD bound to the tip, release-grade token.

## 3. Sequencing ruling for the in-flight shadow-fit run

QA's bootstrap/LOAO computation was launched before this correction
order and is still running under checkpoints. Disposition, honoring
"no evidence under a contradictory criterion" without wasting
honest computation: the COMPUTATION may finish (the checkpointed
resample arithmetic does not depend on the partition sentence); the
CLOSURE-READ ARTIFACT — the report layer that applies the 3/20/23
partition and the event-support accounting — must be generated
fresh from a generator commit that POSTDATES this correction
commit, recomputed from the checkpoints. The ancestry chain then
reads true: corrected criterion -> report generator -> evidence.
If the harness cannot regenerate the report layer separately from
the resampling, the run reruns in full. Either way, no closure
read exists whose generator predates the unambiguous criterion.

## 4. Standing

Census closed at 23; full tier authorized; old path
BLOCKED/ADVERSE; mechanism supported at 4, not closed; R26-1 one
physical approach, restamp HELD OPEN, count 28 unreconstructed;
bridge open; repair shadow-only; cohort-4 HOLD; Sakana STANDBY;
sigma_a_cfg 0.35. GO items running: archaeology, verdict taxonomy,
applicability ledger, and — after this commit — the re-score and
the shadow-fit closure read.
