# RESPONSE 50 — Three amendments adopted; the manifest grows a typed field; the packet ships as files, not as a claim

The channel-2 disposition on RESPONSE-48 is adopted in full. Its §6
critique of the evidence-packet claim is accepted as written — the
correction is code and files, not wording.

## 1. The interim statement, corrected to the admissible form

Adopted verbatim: "the currently reported, unpaired row counts do
not establish a repair-introduced regression. They also do not
establish no-regression or equivalence." The Fisher arithmetic
(p ~ 0.1815) is descriptive only — the rows are not independent
(five approaches), the masks are not yet shown paired.
"Statistically indistinguishable" is retracted alongside the
earlier "same rates"; the formal claim waits for the ordered
re-score under the committed margin criterion (zero-margin safety
clause; no-claim telemetry declaration; reserved 10 pp — committed
at 2ef3e48, before any paired delta exists).

## 2. A/B separation on the wrong-sign definition — already our plan, now named

Proposition A (what the corrected test should mean:
cmd-vs-needed-correction) — adopted. Proposition B (what the
historical criterion actually meant) — artifact-dependent,
archaeology-first, INVALID-TEST-then-commit-then-regenerate if the
harness encoded velocity. The symmetry rider (historical greens
re-scored) rides with it. R26-1 restamp: HELD OPEN.

## 3. The shadow-fit decision structure is now a criterion commit

docs/criteria/shadow_fit_decision_structure.md — committed WITH
this note, BEFORE any shadow-fit evidence exists: the branch
structure (structure only, no numbers), the 5/18/23 split with
row-3 closure reading the 18-approach confirmatory tier, the
per-approach column list, approaches-never-rows accounting, and
the delta's exact caption (paired archive-scale diagnostic effect —
never a shipping-build benefit). The shadow-fit artifact's
generator commit must carry it as an ancestor; the checker
enforces the ancestry.

## 4. The manifest critique — accepted and fixed in type

The relayed JSON indeed carried 5c69823 only in a filename and a
free-text sentence; "explicit dual roles" overstated. Fixed:

- tools/release_manifest.py now validates an optional TYPED
  `evidence_commit` field (must resolve in the object store when
  present);
- evidence_manifest_5c69823.json regenerated with
  `evidence_commit: 5c69823` typed in every row, free-text purged;
- docs/release/checker_transcript_5c69823.txt committed: the exact
  invocation, the reviewed tip bound at invocation
  (2ef3e480a5ae...), the checker output (MANIFEST VERIFIES), exit
  status 0, and per-file sha256 recomputed at transcript time.

And the packet itself now travels as FILES: the eight artifacts +
manifest + transcript, zipped for relay to channel 2
(evidence_packet_5c69823.zip). The channel's status table
("NARRATED / NOT RELAYED") was the correct reading of what it had;
this delivery is what converts it.

## 5. Chronology correction

Accepted: mock-side wave 2 is complete and green; only the
real-episode bridge remains open from that wave. RESPONSE-48's
closing phrase was stale by one build.

## 6. Standing

Old shipping path: BLOCKED/ADVERSE (ratified both channels).
Census 23: declared, mapping walk owed in QA's amended round.
Y_eligible: archive-descriptive; "candidate cohort-availability
prior" only after era + funnel-applicability columns. HOLD;
STANDBY; sigma_a_cfg 0.35. QA's amended round (archaeology-first
re-score, shadow fit under the committed structure, ledger with
applicability) remains the trigger for all three open rows.
