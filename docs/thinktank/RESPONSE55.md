# RESPONSE 55 — Branch two fires: the shadow barely moves the bound; the remainder has a face (slope structure, not intercepts); the 28 is reconstructed and it was never the velocity comparison

QA's shadow-residual diagnostics landed (aab5508, artifact at
tuning/shadow-residual-DIAGNOSTIC-385b7eb-20260720T202828Z/).
Generator commit 385b7eb PREDATES every criterion commit — by the
standing rulings this is NON-ADJUDICATIVE ARITHMETIC, read as
diagnostics only; the closure read regenerates post-d0a73f0. What
the arithmetic says is nonetheless the campaign's pivot.

## 1. The pre-registered second branch fires

Shadow conservative U95 = 1.710 vs old 1.735; shadow point 1.199
vs 1.307. **The latch repair barely moves the archive-scale
bound.** Under the committed decision structure: U95 > 0.35 -> DO
NOT MOVE; the remainder is NAMED with a falsifiable prediction
before anything else advances. The paired archive-scale diagnostic
effect (the repair's measured benefit) is real but small —
b0 collapsed exactly where the mechanism predicted (the low-auth
discovery cluster moved -0.913) and nowhere else, because most of
the 23 latched at auth ~ 1.0 where the mechanism predicts nothing.

## 2. The remainder's face, read from the per-cluster table

- **The variance driver is SLOPE structure, not intercepts**: per-
  cluster b1 (signed age slope of the residual) spans roughly -4.4
  to +3.7 m/s per second across the 23, sign-heterogeneous,
  auth-independent. At these slopes a 0.3 s age carries ~0.2-1.3
  m/s of mean residual — this, not intercept offsets, is what a
  variance fit reads as sigma_a ~ 1.2.
- **The intercept story also has non-latch structure at scale**:
  auth ~ 1.0 clusters with |b0| in the 0.32-0.44 band exist in both
  signs (A071 +0.43, A091 -0.44, A100 -0.32). The latch mechanism
  remains supported FOR WHAT IT CLAIMED (transition-attenuation
  intercepts; its refutation pattern was large SAME-SIGN b0 at
  auth ~ 1, and the signs here are mixed) — but it is now formally
  what the five-cluster round suspected: not the whole story.
- **A consistency anchor for free**: the ONE physical TERM episode
  in the archive (A091 = 20260719T201851) shows b0 = -0.4449 — the
  same -0.437-class signed offset the very first decomposition
  found on live-era data. The number that started this arc
  reappears in the widest table exactly where it should.

## 3. The candidate second mechanism — named, with its falsifiable prediction

Submitted for both channels as a PRE-REGISTRATION, not a finding:
**unmodeled legacy vertical actuation during the withheld
window.** Nearly all 23 approaches come from eras where TERM never
actuated — the vertical was driven by the legacy hold. The
forced-withhold evaluation freezes an anchor and prices deviation
as drift; but in these recordings the vehicle's vertical rate was
being actively COMMANDED by a controller the anchor model does not
see. If so, the archive-scale sigma_a is not anchor physics — it
is the legacy controller's activity read as drift, an
overestimate by construction (the evaluation-side cousin of the
R26-3Q scope limit).

Falsifiable prediction, testable from existing CSVs with no new
replay: **per-cluster |b1| correlates with the recorded legacy
vertical-command activity inside the withheld window** (e.g., RMS
of the logged applied vertical); near-hover clusters should show
small |b1|; and clusters whose logged vertical activity is fed to
the feed-forward as applied reference should collapse their b1.
If auth-1.0 clusters with near-zero legacy activity still carry
|b1| ~ 2, the mechanism is refuted and the drift is real — with
everything that implies for the gate.

## 4. The 28 reconstructed — proposition B answered

The archaeology found the historical formula:
`cmd * e_meas < -1e-6` — command against RAW MEASURED e, ZERO
command deadband, reference deadband 0.03. It reconstructs the 28
exactly (28/68 rows). So the historical criterion was NEVER the
velocity comparison — it was needed-correction FAMILY, but with
two defects: the reference was the raw measurement rather than the
applied correction target, and a zero command deadband let
near-zero commands score against measurement sign noise. Under the
committed registered criterion (cmd vs applied_e_z, deadbands
0.02): **old 0, new 0 violations**. The channels now adjudicate
the historical verdict's status (INVALID TEST vs underspecified);
QA correctly altered no verdict. My earlier 24/16 reconstructions
against velocity were numerical coincidence — entered in the
ledger as such.

## 5. Sequencing and what regenerates

This artifact is diagnostics; the ADJUDICATIVE round regenerates
under a post-d0a73f0 generator with: the 3/20/23 closure tables
(20-confirmatory profile/bootstrap/LOAO/U95 resampled on the
REGISTERED twenty, never relabeled); the event-support wrong-sign
scorecard (9 events / 7 sign-evaluable / 2 zero-on-support) with
the fixture-level verdict language; the full checkpoint-lineage
field list (checker-enforced); manifest rows + transcripts through
the self-binding contract; PLUS the new §3 prediction table
(per-cluster b1 vs legacy vertical activity). Y_eligible-by-era
and the frozen 2x2 (45 compound-signature positives) are received
for the transport ledger and await the same manifested round.

## 6. Standing

The bound does not move; the HOLD was never closer to lifting and
the process was never healthier: the repair did exactly what its
mechanism promised, the promise was just smaller than the problem.
Old path BLOCKED/ADVERSE; repair shadow-only (its live case now
rests on the second mechanism's adjudication); census 23; R26-1
restamp awaits the clean-criterion re-score; cohort-4 HOLD; Sakana
STANDBY; sigma_a_cfg 0.35 — and looking more load-bearing than
ever.
