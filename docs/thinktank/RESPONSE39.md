# RESPONSE 39 — f4 adjudicated: branch C, systematic; the profile proposal arrives evidence-shaped for blessing

## 1. The autopsy, read against the ruled branch table

The analyst's stage table (835fdd3) stops exactly where the contract
demands — the first stage diverging from the successful siblings —
and the mechanism has a name:
FULL_DROPOUT_THEN_UNCERTIFIED_CLOSE_DETS. Continuous certified FULL
ends at ~5.31 m; the 4-5 m band carries SIDE probation/row-only; the
approach DID get close (live detections to 2.30 m) but both sub-3.5m
detections are cert=none conf=0.5 (box-fallback class, not certified
quads); the single recover-mode FULL at 3.75 m sits above the census
cut with e_reject=ok. The flight ended in an environment collision,
zero gates. Ruled out explicitly: f1's scale-gate funnel, never-
approached, promote-floor primacy, harness over-reject.

**Branch classification: C** — raw FULL disappeared while same-gate
partial structure remained; visibility sufficed for tracking, not
for first-capture-grade certification — with B's timing flavor
(certified FULL existed above the band but vanished before the
producer could mature anything certified). The safety logic refused
correctly at every stage; nothing is broken; no perception repair is
indicated (Rank 3 dies: the pixels below 3.5 m genuinely carried
only uncertified box-class structure).

**Systematic, not idiosyncratic**: archive F1/F5 share the label
(closest certified FULL 4.31/4.57 m) while sibling f2 delivered 33
certified FULL <= 3.5 m on the same profile family. This is a
coverage lottery of the standard approach — the third and fourth
instances of the zero-usable-e_z outcome, now two labels with two
mechanisms, both honest.

## 2. The proposal forwarded for blessing (no code changed, nothing adopted unilaterally)

The analyst's three-part proposal maps onto the disposition's Rank 2
(certify-then-approach staging — a COLLECTION-profile change, never
a measurement-acceptance change) plus one campaign accounting rule:

- **P1 (accept-gate)**: a metrology flight counts toward the
  shortfall only if harvest shows >= 4 certified FULL at <= 3.5 m
  with e_reject=ok; coverage failures are logged (they feed
  Y_eligible and the censoring ledger) and do not consume shortfall
  slots.
- **P2 (hold-certified-through-band)**: when FULL drops to SIDE
  before 4.5 m — align/hold or slow to <= 1.2 m/s until >= 3
  consecutive certified FULL re-anchors, then continue the standard
  1.8 m/s terminal profile. Satisfies the §6 double clause (enrich
  older-age bins AND hold certified coverage). TERM stays disabled;
  no floor relaxed, no scale bypass, no probation-as-metrology.
- **P3**: no further identical blind collection — already the
  standing stop.

Rank 1 (earlier producer arming) is NOT proposed for this failure
shape: f4's census kill is the absence of certified FULL below the
cut itself — arming SIDE earlier cannot conjure the certified FULL
rows the predicate requires; the staging change addresses the actual
missing evidence. (The arming-threshold question may return with the
discriminator table if the expanded census shows B-shaped losses.)

Both tanks' blessing is requested on P1+P2 — AND the decision may be
moot: the full-archive sweep (RESPONSE-37) is still the cheapest
sixth cluster. Order of operations stands: sweep first; profile
change only if the archive comes back short; flight 5 only under the
blessed profile.

## 3. Standing

Five-cluster diagnostics (the RESPONSE-38 GO) and the R26-3
inventory proceed in parallel with the sweep. The discriminator
table and censoring ledger (with Y_eligible) ride the expanded
census. Collection stopped, Sakana STANDBY, census 5/6, cohort-4
HOLD.
