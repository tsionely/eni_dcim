# RESPONSE 22 — The switch amendments and the conservative door, implemented

The RESPONSE19 disposition's pre-flight amendments are in the build
(176/176):

- **Paired-overlap switch**: downgrade now requires ≥3 paired
  unique-exposure FULL/SIDE overlaps with the newest pair fresh, and
  the **HARD |median Δe| ≤ 0.10m** step limit — the old
  max(0.10, 3σ_side) rule (which permitted 0.225m) is retired and
  PINNED as a regression test (a 0.15m bias that was legal before
  must now block). Rate compatibility enforced: opposing slope signs
  at the transition forbid it (deadband 0.05).
- **The one-shot grace is diagnostic-only**: it lifts exactly the
  single temporal-innovation alarm of a certified ≤0.10m step
  (bounded 0.10 + jump floor), and touches no admission, envelope,
  maturity, age, phase, or ownership logic.
- **Conservative first-live authority**: only FULL_QUAD may open the
  control door; a mature, honest SIDE_PAIR-active oracle maintains an
  inherited owner but never first-captures — pinned as a fixture.
- **Probation is out of metrology**: only CERTIFIED observations
  enter the readiness tail (the pinned tail definition's
  no-bridging-probation rule, implemented at the door).

Remaining before cohort 4, as ruled and already routed: L1 as SHADOW
CAPTURE (ready + admission ≤ corridor + shadow owner TERM in
POSITION with valid provenance — not readiness alone), the earned
side sigmas from the perception replay, ψ-age fields + S4
disposition, the full telemetry set, the certificate-boundary audit
(pinned semantics: promote ≥1.6m / maintain-only 1.4-1.6m /
continuity-only <1.4m), and S1/S4/S5. The A8 independence amendment
(clear-and-re-approach defines n; 100ms de-bounces chatter) matches
the analyst's own harvest treatment (F2 counted as one episode);
chain stays HELD. Cohort 4 stays HOLD until every gate row is green.
