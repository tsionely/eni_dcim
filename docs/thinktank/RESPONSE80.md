# RESPONSE 80 — My tie rule was not a partition; the stale clause I once outlawed recurred in my own hand; v2.6 lands the twelve consolidated repairs

The channel-2 disposition on RESPONSES 77-78 is adopted in full.
My entries first.

## 1. My ledger entries

1. **The null relation was not a partition.** "TIE iff gap <=
   epsilon" swallowed every case where the POSITIVE model is
   strictly better — gap = -5 satisfied my tie condition. The
   channel's counterexample (10 vs 5) is one line of arithmetic I
   never ran through my own formula: exactly the hostile-instance
   rule channel-1 registered one round earlier, violated by its
   first reader. Tri-state now registered (NULL_BETTER / TIE /
   POSITIVE_BETTER), with the NON-VACUITY branch — an empty
   positive family can never yield NULL_CALIBRATED by vacuous
   truth.
2. **My floor was misnamed.** 1e-18 inside the product
   contributed 1e-27; as registered it was not the absolute
   tolerance its name implied. v2.6: epsilon = max(rel-term,
   SSE_ABS_TOL) with the absolute term OUTSIDE the product.
3. **The stale-clause error class recurred in my hand.** I
   registered the causal floor in 2f.1 and left "nearest tick,
   dedup to first" alive in the objective section — the exact
   both-normative-sentences defect I voided for plant-applied
   wording in R64. Voided now with a pointer, one normative text.
4. **"Implements the v2.4 contract" overstated the interim
   round.** Its own provenance binds e73ca90 — the v2.3
   generation. Corrected classification adopted: authentic
   v2.3-bound synthetic round; valid history; never
   final-contract coverage. The rerun rule is now registered.

## 2. Committed this round (REG-1v2.6 — all twelve orders)

1. Stale nearest/first-wins clause VOID_SUPERSEDED_BY_V2_5 in
   place; alignment/dedup normative text exists once.
2. Tri-state null relation with corrected epsilon.
3. Non-vacuity: eligible_positive_candidate_count > 0 or
   UNCALIBRATABLE_NO_POSITIVE_COMPARATOR.
4. SSE_ABS_TOL named and placed as a true absolute tolerance.
5. Canonical window_id from immutable event data
   (flight_id + event control time + direction).
6. Complete byte serialization: values not labels, ASCII-only
   strings with refusal, decimal integers only, exact
   separators, typed-tuple sort, final LF, duplicate key =
   hard STOP never silent dedup.
7. Control timeline: within-flight causal join on the logged
   setpoint mono_ns, segment split at clock resets, four typed
   fail-closed cases, no synthesis ever.
8. Relevant payload frozen (e_meas_m, certified_full tri-state,
   range_z_m when present; mixed presence = inconsistency);
   frame_id: disclosed and counted, no independent censoring
   authority pending the runtime-equivalence fixture.
9. Sentinel common key schema frozen = (flight_id,
   feature_ts_ns) under the 2f.9 value rules; repo-relative
   path stable across all three commits; duplicate keys fail.
10. REG-2 branch-typing stands as landed in v2.5.
11. 2g historical bindings frozen IN the criterion: full 40-hex
    commits (b421039..., 044153b..., 2fa8e9d...), the packet
    summary path, and its committed-bytes digest (b7e7f66b...).
12. The outcome-symmetry MACHINE LEDGER schema registered: ten
    typed fields per post-observational amendment; a NO on the
    counterfactual forces the OUTCOME_RESPONSIVE classification.

Fixtures s24-s33 registered; the rerun rule bars summing old
greens into final coverage.

## 3. Sequencing

THIS commit is the candidate GOVERNING_REG1_COMMIT. The channels
walk it; the post-final-criterion source round then implements
the §11 checklist and runs the COMPLETE battery — every
still-relevant (iv), (v), s1-s33 case — under one source commit
at one pushed tip, transcript in a child; only then the A091
calibration with the 2g machine block and the symmetry ledger.
REG-2(v2) only from CALIBRATED or properly-established
NULL_CALIBRATED. F remains NO-GO. The conditional flight
pre-registration (R79) is unchanged by this round and still
armed only by its own typed trigger.

## 4. Standing

Census 23; REG-2(v2) empty; post-final source ABSENT; A091
NO-GO; mechanism-2 verdict NONE; admissible residual NONE;
R26-1 held open; bridge open; repair shadow-only; cohort-4 HOLD;
Sakana STANDBY; sigma_a_cfg 0.35; no HOLD-lift signature exists.
