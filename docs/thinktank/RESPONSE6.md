# ROUND CLOSE — analyst results on the round-3 asks (both think tanks)

Paste to both tanks. This closes the measurement round; the next inputs
we bring are live results from the terminal-ownership build.

## 1. Test A final verdict: the feature is COHERENT, the calibration isn't

Rerun with R4-measured d* and a bar/banner identity split (scale-gate
kills labeling strips): F2's top_bar bucket shows the pure-offset
signature — median +0.82 m at 100% sign accuracy. A single constant
separates it from release; F1 remains identity-confused (banner). Verdict:
the W·y/ℓ oracle survives; free-standing mask extraction does not. The
vertical channel stays unwired until the close tracker's projected-model
identity stage supplies y/ℓ with verified structure identity and
exposure-time de-rotation — exactly the integration order both tanks
specified. The residual calibration constant will be measured, not
assumed (V6 moments rule).

## 2. The wrong-side detector works on the real failure (V2 validated offline)

F2's fiction moment, one-line check: believed ty at the detection's
depth predicts image row 253; the actual quad center sat at row 123 —
Δ130 px, BELIEVED_ROW_DISAGREES. This is the runtime monitor's
discriminative power demonstrated on the exact case that killed the
flight. V2's runtime implementation (row/ordering violations against
the raw mask -> early abort) is the next perception task after the
terminal-ownership build is confirmed in flight.

## 3. H3 census: the side bars DO outlive everything (tank-1 §0 confirmed)

F1's last-1.5m visible-edge order: left -> right -> bottom -> top, with
a SIDE BAR as the last surviving structure — matching the side-bar
separation claim (and our H5 test showing the tracker extracts absolute
range from exactly that pair). F2: banner appears mid-approach with
state HIGH — the structure-identity-as-vertical-measurement note holds
there too.

## 4. T3 formally closed

Full-log replay: F1's gap entry vetoed the sink insurance while the
hold was already climbing (gap_bias=0); the double climb is
structurally unreachable. Shipped rule, offline-proven, done.

## 5. Live context for the next round

The terminal-ownership build (34d4f6b) is about to fly: it fixes the
NEW failure mode found in the first verified full-approach cycle — at
terminal range the next gate (seen through the near opening, threaded
by the racing line) stole the candidate contest; the lock followed;
the drone clipped the near frame (closest 0.88m, 4 gate clips) and the
post-impact stale lock rejected reality. Fixes: prediction-consistent
candidate boost (beats the cyan prior inside 6m), collision clears the
lock. Next round's questions will come from that cycle's slices.
