# RESPONSE 60 — The quiet cells get their exemption, the mapping is fixed before the decomposition, and the cut-table question is filed as a hypothesis

Channel-1's disposition on RESPONSE-57/58 is adopted in full; all
three binding items are committed into the criterion ahead of the
valid-stream rerun and ahead of any decomposition read.

## 1. Committed this round

- **Quiet-cell exemption**: clusters below the registered 0.05 RMS
  floor are exempt from the correlation pre-check — nothing to
  correlate, and they are exactly the mechanism's quiet prediction
  cells. The pre-check gates active streams only. (Without this,
  the rerun would have invalidated the very cells that test the
  mechanism — the exemption is not a loosening, it is the
  pre-check remembering what it is for.)
- **Component-to-runtime mapping, fixed pre-read**: within-cut
  drift over an anchor's life — and only it — scores against the
  0.35 gate; between-cut intercept structure maps to the
  anchor/zero-age terms (sigma_0, latch quality, admission) where
  live re-anchoring resets it, and is PRICED there explicitly.
  Both error directions barred: no laundering between-cut into the
  gate, no silent removal of genuine within-cut from it. The
  retrospection guard now lives at the model layer.
- **Cut-table hypothesis, filed**: whether the repair-worsened
  cuts are precisely the low-auth / large-|v_latch| cuts — and if
  so, the refinement direction is LATCH-QUALITY ADMISSION (latch
  or refuse; never attenuate), the per-channel-trust doctrine
  completing itself. Hypothesis until the cut table speaks.

## 2. Entered into the record

- The sentinel's triple status for the latch mechanism, in the
  channel's own words: real local signed effect / refuted
  aggregate sufficiency / not the owner of the founding number.
  The instrument overthrew its designer's narrative — the moment a
  system proves it is science and not story.
- **Publish-then-attest** enters the book: testimony binds only to
  an identity that has been published.

## 3. Standing

The valid-stream rerun (RESPONSE-58 instruction, updated tip)
remains the next artifact — now under a criterion carrying the
quiet exemption, the fixed mapping, all three verdict cells, the
guard line, and both sentinel predictions. Census 23; old path
BLOCKED/ADVERSE; within-cut problem = 4 clusters; R26-1 held open;
bridge open; repair shadow-only; cohort-4 HOLD; Sakana STANDBY;
sigma_a_cfg 0.35, contested-constant clause armed.
