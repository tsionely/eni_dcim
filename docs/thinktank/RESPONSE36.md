# RESPONSE 36 — P4(d) closes green; the three binding items shipped; flight 4 repeats the zero-e_z outcome and collection STOPS

## 1. Advisory-20 adopted; the three binding items are code, not intentions (191/191)

- **Terminology amendment**: LOCKED_IDENTITY_REVOKED /
  SUCCESSOR_PROBATION adopted into the certificate's forensic
  vocabulary (docstring at the single revoke call site). The runtime
  never emitted a successor-certified claim; now the words can't
  either. "Honest relock" remains the forensic classification only.
- **Negative-control triplet**: shipped as permanent fixtures beside
  the P4(d) one. (a) one grossly inconsistent side-pair measurement
  demotes its tick and the identity recovers IN-CHAIN without a
  re-anchor; (c) SIDE silence degrades by time decay and a detector
  re-anchor restores the held identity; (b) persistent gross target
  contradiction may revoke — the certified-detector branch, which a
  call-site audit confirms is the ONLY revoke wire in the tree: the
  SIDE producer cannot revoke by construction, and now by fixture.
- **Purity invariant**: shipped. Strip-the-instrument A/B — identical
  fixtures with the shadow channel live vs absent return the
  bit-identical actuating triple (owner, v_bz, vz_up) and identical
  actuating oracle/arbiter state; only shadow fields differ. The
  second guidance call is pure scalar math, as required.
- **Algebra pin**: delta_latch = v_anchor_old − v_full_raw is already
  the shipped convention (identical to the ruling's). The e_cross
  split is asserted definition-proof against the code's
  crossing_error rather than a document-side sign (the fixture pins
  h·delta with the sign the code's convention produces; the physics
  is the ruling's identity under its residual convention).

## 2. The analyst's two results

- **f1 autopsy: NOT RESCUABLE — and the gate was right.** All 11
  sub-3.5m FULL fixes die at the scale gate:
  SCALE_GATE_FLIPPED_FAR_GATE_POST_GAP — a far-gate right-edge blob
  believed near-range in recover mode after a FULL gap (stale age
  1.2-1.5s, span ~1/3 of the honest fx·W/Z; the same blob later
  settles at ~10m and reads ok). Promote-floor, bloom, and
  harness-over-reject ruled out; archive twin 071220 same kill. Per
  the ruling: this is genuine perception behavior, not a defect —
  there is no general repair to write because nothing is broken; f1
  stays excluded and enters the liveness/failure fixture library.
  No 7th cluster.
- **Phase-wire blast radius: NO_CHANGE.** 699 archived TERM-owned
  rows, zero first-captures at tau <= 0.45 — the RESPONSE-34 §3
  retro claim is now audit-confirmed, not asserted.

## 3. Flight 4 failed; the escalation rule fires; collection STOPS

Flight 4 (20260720T142917) produced ZERO certified FULL below 3.5m
(SIDE probation/row-only throughout) — reason label
NO_CERTIFIED_FULL_BELOW_3P5, distinct from f1's scale-gate kill but
the same zero-usable-e_z outcome. The ruled escalation ("a repeated
signature blocks further identical collection") is honored on the
outcome, not the label: **no flight 5 until f4's autopsy names its
mechanism.** Two of four metrology flights have now censored
themselves through two different funnels; flying more of the same
profile without understanding the second funnel would reproduce
known censoring, not add clusters. The autopsy is tasked (same
stage-by-stage table as f1's; the archive's F1/F5 carry the same
label and serve as comparators). If f4's mechanism is also
honest-gates-working, the metrology profile itself needs a
selection change (e.g., the §6 guidance: profiles chosen to enrich
older-age bins AND hold certified coverage) — a tanks-visible
decision, not a quiet retune.

## 4. R26-2/3 split — adopted; the R26-3 evidence inventory starts now

R26-2 may close from the six-cluster harvest; R26-3 requires
physically realized, owner-qualified TERM command regimes. Tasked to
QA in parallel with the census wait: inventory ALL existing
TERM-owned recordings (cohort-3 era and the R26-1 deep fixture) for
which command regimes they physically contain (up/down steps,
triangular returns, saturation, authority-limited), with provenance
columns. What the inventory cannot cover routes to the closed-loop
simulator fixture option — scoped only after the gap list exists.

## 5. Board (per the disposition's updated form)

Rows 1-2 GREEN (closed by the ruling). Row 3-9: pending the sixth
cluster — census holds at 5, collection stopped pending the f4
autopsy. Row 10: R26-2 pending harvest; R26-3 pending the physical
command-regime inventory. Rows 11-12 standing (12's dual-read
fields now complete AND purity-pinned). sigma_a_cfg stays 0.35 for
first live regardless of the fitted bound. Cohort-4 HOLD.
